You are upgrading the entire Studio Flow platform to production‑ready status.
Perform the following tasks in order, making all necessary code changes, SQL migrations, Supabase updates, and environment variable integrations.

1. AUTO‑CREATE PROFILES ON SIGNUP (SUPABASE TRIGGER)
Create a Postgres trigger on auth.users that automatically inserts a row into public.profiles whenever a new user signs up.

The inserted row must include:

id = NEW.id

email = NEW.email

username = split_part(NEW.email, '@', 1)

display_name = split_part(NEW.email, '@', 1)

subscription_active = false

role = 'user'

Implement:

handle_new_user() function

on_auth_user_created trigger

Ensure the function uses SECURITY DEFINER.

2. ADMIN BYPASS RLS (SUPABASE)
Add a new RLS policy on the submissions table:

Policy name: "admin_bypass"

Applies to: FOR ALL

Role: authenticated

USING: auth.jwt() ->> 'role' = 'admin'

WITH CHECK: auth.jwt() ->> 'role' = 'admin'

Ensure this policy does not conflict with existing policies.

3. STRIPE → SUBSCRIPTION_ACTIVE (SUPABASE EDGE FUNCTION)
Create a Supabase Edge Function named stripe-webhook that:

Validates Stripe webhook signatures

Handles the following events:

checkout.session.completed → set subscription_active = true

customer.subscription.deleted → set subscription_active = false

customer.subscription.canceled → set subscription_active = false

customer.subscription.unpaid → set subscription_active = false

Use:

SUPABASE_SERVICE_ROLE_KEY

STRIPE_SECRET_KEY

STRIPE_WEBHOOK_SECRET

Update the profiles table using:

ts
supabase.from("profiles").update({ subscription_active: true }).eq("id", userId)
or false depending on event.

Add logging for debugging.

4. FULL RLS AUDIT + FIXES (SUPABASE)
Audit all RLS policies across:

profiles

submissions

contests

contest_entries

votes

events

custom_events

Ensure the following rules:

profiles
SELECT: user can read their own row

UPDATE: user can update their own row

INSERT: only via trigger

DELETE: admin only

submissions
INSERT: user must be authenticated AND subscription_active = true

SELECT: public

UPDATE: owner or admin

DELETE: owner or admin

contests
SELECT: public

INSERT/UPDATE/DELETE: admin only

contest_entries / votes
INSERT: authenticated

SELECT: public

UPDATE/DELETE: owner or admin

Ensure no conflicting policies remain.

5. FIX VIDEO PIPELINE (FRONTEND + BACKEND)
Resolve all video upload and playback issues.

Perform the following:

A. Fix MIME types
Ensure uploads specify:

ts
contentType: "video/mp4"
Ensure Supabase bucket MIME mapping includes:

.mp4 → video/mp4

.mov → video/quicktime

B. Fix signed URL logic
If bucket is private:

Generate signed URLs on the server

Use them in the frontend player

C. Fix Netlify ffmpeg issues
If any serverless function uses ffmpeg:

Move media processing to a Supabase Edge Function
OR

Use a dedicated server / Replit background worker

Remove any broken fluent-ffmpeg imports from Netlify functions.

D. Fix submission pipeline
Ensure:

Video uploads to Supabase storage

Submission row inserts into submissions

Video URL is stored in the row

Player loads the correct URL

Add error handling and logging.

6. ENVIRONMENT VARIABLES
Ensure the following environment variables exist and are used correctly:

SUPABASE_URL

SUPABASE_ANON_KEY

SUPABASE_SERVICE_ROLE_KEY

STRIPE_SECRET_KEY

STRIPE_WEBHOOK_SECRET

NEXT_PUBLIC_SUPABASE_URL

NEXT_PUBLIC_SUPABASE_ANON_KEY

7. CLEANUP & VALIDATION
After implementing everything:

Run a test signup → confirm profile auto‑creates

Run a Stripe test payment → confirm subscription_active updates

Run a contest submission → confirm RLS passes

Upload a video → confirm it loads

Test admin → confirm bypass works

Test non‑subscriber → confirm submission is blocked

Fix any issues found.

8. OUTPUT
When finished, output:

All modified files

All new files

All SQL migrations

All environment variable changes

A summary of what was fixed

A list of remaining TODOs (if any)

⭐ END OF REPLIT AGENT PROMPT