You are updating the Studio Flow frontend to fix incorrect sidebar routing and ensure contests appear in the correct location.

GOAL:
Move all contests out of the "Subscription" sidebar section and ensure they appear ONLY on the new /contests page, powered by the new Supabase views.

---------------------------------------
STEP 1 — REMOVE OLD SIDEBAR CONTEST LIST
---------------------------------------

Search the entire codebase for any sidebar logic that:
- hardcodes contest names under "Subscription"
- maps contests into the Subscription menu
- loads contests using old tables (contest_entries, contest_votes, etc.)
- uses legacy contest arrays or static lists

Delete or rewrite this logic so the Subscription sidebar ONLY shows:
- membership tiers
- subscription-related items

Contests must NOT appear under Subscription.

---------------------------------------
STEP 2 — FIX ROUTING FOR CONTEST LINKS
---------------------------------------

Search for any routes like:
- /subscription/[contestName]
- /subscription/contest/*
- /subscription/contests/*
- or any dynamic route that maps contests under Subscription

Replace them with:
- /contests
- /contests/[id]

Ensure all contest links route to the new contest detail page.

---------------------------------------
STEP 3 — UPDATE THE CONTESTS PAGE DATA SOURCE
---------------------------------------

Ensure the /contests page loads contests using:

supabase.from('admin_contest_dashboard').select('*')

or, for public view:

supabase.from('contest_leaderboard').select('*')

Remove any old queries referencing:
- contest_entries
- contest_votes
- contest_prizes
- contest_results
- archived/voting/completed states

---------------------------------------
STEP 4 — DISPLAY CONTESTS ON THE PAGE
---------------------------------------

Ensure the /contests page:
- maps the results into contest cards
- shows title, category, submission_count, total_like_count
- shows contests even if they have 0 submissions
- does NOT require any old fields like voting, archived, or completed

---------------------------------------
STEP 5 — CLEAN UP TYPES & COMPONENTS
---------------------------------------

Remove or update any TypeScript types that assume:
- contests have voting states
- contests have archive states
- contests have entry counts from old tables
- contests belong under Subscription

Ensure all components use the new contest types from:
- admin_contest_dashboard
- contest_leaderboard

---------------------------------------
STEP 6 — VALIDATION
---------------------------------------

After changes:
- The Subscription sidebar should show ONLY subscription items.
- The Contests page should show ALL contests.
- Clicking a contest should route to /contests/[id].
- No component should reference old contest tables or old contest states.
- The app must compile with zero TypeScript errors.

---------------------------------------
FINAL OUTPUT
---------------------------------------

After completing the work, print:
- A list of all modified files
- A list of removed legacy sidebar logic
- A list of updated routes
- A list of updated Supabase queries
- Any remaining references requiring manual review
