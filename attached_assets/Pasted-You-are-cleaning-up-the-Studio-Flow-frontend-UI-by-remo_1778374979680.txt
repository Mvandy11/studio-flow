You are cleaning up the Studio Flow frontend UI by removing outdated and duplicated contest and event components.

GOAL:
1. Remove all static contest entries under the “Subscription” sidebar section.
2. Remove the old hard-coded Events block from the Dashboard.
3. Ensure contests appear ONLY under the /contests page.
4. Ensure events appear ONLY under the Events page.
5. Do NOT modify backend code.

TASKS:

1. SIDEBAR CLEANUP
   - Open the sidebar configuration file (likely src/components/Sidebar.jsx or src/config/sidebar.js).
   - Locate the “Subscription” section.
   - DELETE all static contest items such as:
       “Funniest Baby Moments”
       “Worlds Funniest Video”
       (and any duplicates)
   - Ensure the Subscription section contains ONLY subscription-related items.
   - Ensure the only contest link in the sidebar is:
       { name: "Contests", route: "/contests" }

2. DASHBOARD CLEANUP
   - Open the Dashboard component (likely src/pages/Dashboard.jsx).
   - Locate the old hard-coded Events list (Creator Showcase Night, Beat Battle Live, Comedy Open Mic, etc.).
   - DELETE the entire block.
   - Do NOT replace it with anything.
   - Dashboard should show only:
       - Status widgets
       - Quick links
       - Optional summary counts
       - But NO events and NO contests.

3. ROUTING CHECK
   - Open the router file (src/App.jsx or src/routes/index.jsx).
   - Ensure the only contest routes are:
       /contests
       /contests/:contestId
   - Ensure the only events routes are:
       /events
       /events/:eventId
   - Remove any leftover routes pointing to old components.

4. FILE CLEANUP
   - Search the codebase for any file containing:
       “Creator Showcase Night”
       “Beat Battle Live”
       “Comedy Open Mic”
       “Funniest Baby Moments”
       “Worlds Funniest Video”
   - Remove or delete any unused components or static data files.

5. VERIFY
   - Run the frontend.
   - Confirm:
       - Sidebar no longer shows contests under Subscription.
       - Dashboard no longer shows old events.
       - Contests appear ONLY on /contests.
       - Events appear ONLY on /events.
       - No duplicates anywhere.

6. Commit only the cleaned-up frontend files.
