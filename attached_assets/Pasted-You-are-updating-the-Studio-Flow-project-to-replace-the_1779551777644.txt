You are updating the Studio Flow project to replace the old event request
system with a new category-based event slot system. Creators with the
creator_50 membership tier can post events directly into categories without
admin approval.

Make the following changes:

────────────────────────────────────────
1. CREATE A NEW TABLE: event_slots
────────────────────────────────────────

Create a Supabase table:

event_slots:
  id uuid primary key default uuid_generate_v4()
  creator_id uuid references profiles(id)
  title text
  description text
  category text
  thumbnail_url text
  video_url text
  is_live boolean default false
  created_at timestamp default now()
  updated_at timestamp default now()

Categories should be simple text values such as:
"Comedy", "Music", "Dance", "Fitness", "Gaming", "Education",
"Cooking", "Motivation", "Kids", "Talk Show", "Tutorials", "Art".

────────────────────────────────────────
2. REMOVE OLD EVENT REQUEST SYSTEM
────────────────────────────────────────

Delete or disable:
- custom_event_requests table
- event approval logic
- admin approval UI
- any backend routes related to event requests

Creators no longer request events — they post directly.

────────────────────────────────────────
3. CREATE FRONTEND UI FOR EVENT CREATION
────────────────────────────────────────

Add a new page: /creator/new-event

Fields:
- Title
- Description
- Category (dropdown)
- Thumbnail upload
- Video upload OR "Go Live" toggle

Only allow access if:
membership_tier === "creator_50"

────────────────────────────────────────
4. CREATE EVENT LISTING PAGES
────────────────────────────────────────

Add:
- /events (all events)
- /events/:category (filtered by category)
- /event/:id (single event page)

Each event page should show:
- Title
- Description
- Category
- Creator info
- Video or live player
- Donation button (use donation Payment Link)
- Chat
- Reactions

────────────────────────────────────────
5. ADD DONATION BUTTON TO EACH EVENT
────────────────────────────────────────

Use this Payment Link:
https://buy.stripe.com/28E14pgpncgofnmbh3b7y0t

Button text: "Support This Event"

────────────────────────────────────────
6. UPDATE CREATOR DASHBOARD
────────────────────────────────────────

Add a section:
"My Events"

List all event_slots where creator_id = current user.

────────────────────────────────────────
7. TESTING CHECKLIST
────────────────────────────────────────

- Creator_50 users can create events
- Member_30 users can view events
- Free users can browse but not join
- No admin approval required
- Events display correctly by category
- Donation button appears on event pages

Make all changes now.
