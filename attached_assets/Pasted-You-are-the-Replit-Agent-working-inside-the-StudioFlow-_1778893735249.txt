You are the Replit Agent working inside the StudioFlow codebase. Your task is to test and validate the winner‑pull endpoint end‑to‑end.

The backend endpoint should be:

POST /api/contests/:contestId/pull-winners

Body:
{
  "eventId": "<uuid>",
  "numberOfWinners": 3,
  "payouts": [
    { "placeNumber": 1, "payoutAmount": 100 },
    { "placeNumber": 2, "payoutAmount": 50 },
    { "placeNumber": 3, "payoutAmount": 25 }
  ]
}

────────────────────────────────────────
1. LOCATE THE ENDPOINT
────────────────────────────────────────
Search for:
- netlify/functions
- api/contests
- pull-winners
- winner logic
- supabase service_role usage

Identify the exact file implementing the endpoint.

────────────────────────────────────────
2. VALIDATE ENDPOINT BEHAVIOR
────────────────────────────────────────
Confirm the endpoint does the following:

1) Fetches eligible users:
select user_id
from ticket_purchases
where event_id = eventId
and user_id not in (select user_id from winner_history)
order by random()
limit 1;

2) Inserts winners into winner_history:
- user_id
- event_id
- contest_id
- place_number
- payout_amount

3) Returns:
{
  winners: [
    { userId, placeNumber, payoutAmount }
  ]
}

4) Uses the Supabase service_role key.

────────────────────────────────────────
3. VALIDATE ADMIN AUTHORIZATION
────────────────────────────────────────
Ensure the endpoint checks:
- supabase.auth.getUser()
- Fetches profile
- Confirms profile.role === 'admin'
- Returns 403 for non-admins

────────────────────────────────────────
4. CREATE A TEST HARNESS
────────────────────────────────────────
Add a temporary test script or route:

/api/test-winner-pull

This should:
- Call the pull-winners endpoint with mock data
- Log the response
- Log any errors
- Verify that winners are inserted into winner_history

────────────────────────────────────────
5. VALIDATE EDGE CASES
────────────────────────────────────────
Test:
- No eligible users remaining
- Partial winner pulls (e.g., asked for 5, only 2 available)
- Invalid eventId
- Invalid contestId
- Missing payouts array

Ensure the endpoint returns meaningful error messages.

────────────────────────────────────────
6. OUTPUT SUMMARY
────────────────────────────────────────
At the end, output:
- Files inspected
- Files modified
- Whether the endpoint works end-to-end
- Whether winners were successfully inserted
- Any bugs found and fixed
