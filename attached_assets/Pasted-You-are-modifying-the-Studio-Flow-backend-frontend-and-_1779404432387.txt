You are modifying the Studio Flow backend, frontend, and database integration to finalize the entire LIVE EVENT workflow. This includes stream keys, RTMP ingest URLs, slot creation, event creation, OBS integration, and creator-facing controls.

Do NOT modify or break any existing free-chat, contest, or membership systems.

────────────────────────────
DATABASE (Supabase)
────────────────────────────

1. Ensure the event_slots table has these columns:

   - id (uuid)
   - event_id (uuid)
   - slot_title (text)
   - upload_password (text)
   - stream_key (text)
   - stream_url (text)
   - status (text) default 'pending'
   - created_at (timestamp)

2. If stream_url does not exist, add it:

   alter table event_slots
   add column stream_url text;

3. If status does not exist, add it:

   alter table event_slots
   add column status text default 'pending';

4. No RLS changes needed if supabaseAdmin is used.

────────────────────────────
BACKEND — EVENT APPROVAL WORKFLOW
────────────────────────────

5. Open routes/adminApproval.js.

6. Update the approval logic so that approving a request:

   - Creates an event row
   - Creates an event slot row
   - Auto-generates a stream_key using crypto.randomUUID()
   - Auto-generates a stream_url using this format:

       rtmp://live.studioflow.tv/live/<stream_key>

     (If the domain differs, use the correct domain.)

   - Sets slot.status = 'pending'

7. Return the created event and slot to the frontend.

────────────────────────────
BACKEND — CREATOR LIVE EVENT WORKFLOW
────────────────────────────

8. Create a new route file: routes/liveEvents.js

9. Add these routes:

   GET /api/live/slot/:slotId
     - Returns slot info including stream_key and stream_url

   POST /api/live/slot/:slotId/start
     - Sets slot.status = 'live'
     - Sets event.status = 'live'

   POST /api/live/slot/:slotId/end
     - Sets slot.status = 'ended'
     - Sets event.status = 'ended'

10. Mount the route in app.js AFTER express.json():

    app.use("/api/live", liveEventsRouter);

────────────────────────────
FRONTEND — CREATOR EVENT SLOT VIEW
────────────────────────────

11. Open the EventSlotView component.

12. Ensure it loads slot data from:

    GET /api/live/slot/${slotId}

13. Display:

    - Slot Title
    - Stream Key
    - RTMP URL (stream_url)
    - OBS Instructions:
        - Server: rtmp://live.studioflow.tv/live
        - Stream Key: <stream_key>

14. Add buttons:

    - “Start Live Event”
      → POST /api/live/slot/${slotId}/start

    - “End Live Event”
      → POST /api/live/slot/${slotId}/end

15. Update UI based on slot.status:
    - pending → show Start button
    - live → show End button
    - ended → show “Event Ended”

────────────────────────────
FRONTEND — EVENT APPROVAL MODAL
────────────────────────────

16. Ensure the modal sends ONLY:
    - slot_title
    - upload_password

17. Remove any frontend generation of stream_key.
    Backend must generate it.

18. After approval, close the modal and refresh the admin list.

────────────────────────────
VERIFICATION
────────────────────────────

19. Approve an event request:
    - Slot is created
    - stream_key exists
    - stream_url exists
    - status = pending

20. Creator opens slot page:
    - Sees stream key + RTMP URL
    - OBS can connect using these credentials

21. Creator clicks “Start Live Event”:
    - Slot status becomes live
    - Event status becomes live

22. Creator clicks “End Live Event”:
    - Slot status becomes ended
    - Event status becomes ended

23. No errors, no missing fields, no schema mismatches.

Make all changes now.
