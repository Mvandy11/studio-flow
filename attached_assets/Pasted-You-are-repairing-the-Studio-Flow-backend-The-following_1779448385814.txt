You are repairing the Studio Flow backend. The following issues must be fixed:

- Stripe subscription status is not updating in Supabase.
- Stripe webhook is not firing or not being processed.
- Event approval is failing.
- Event slot creation is failing.
- stream_key logic is not running.
- supabaseAdmin may not be used correctly.
- Backend routing order may be incorrect.
- Environment variables may not be wired correctly.

Fix ALL backend issues below.

────────────────────────────
1. VERIFY ENVIRONMENT VARIABLES
────────────────────────────

Check Replit Secrets and ensure these exist:

- SUPABASE_URL
- SUPABASE_SERVICE_ROLE_KEY
- SUPABASE_ANON_KEY
- STRIPE_SECRET_KEY
- STRIPE_WEBHOOK_SECRET

If any are missing, add placeholders and comment in code where needed.

────────────────────────────
2. FIX SUPABASE ADMIN CLIENT
────────────────────────────

Open supabase/supabaseAdmin.js and ensure:

import { createClient } from "@supabase/supabase-js";

const supabaseAdmin = createClient(
  process.env.SUPABASE_URL,
  process.env.SUPABASE_SERVICE_ROLE_KEY,
  { auth: { autoRefreshToken: false, persistSession: false } }
);

export default supabaseAdmin;

Search the entire backend for any usage of createClient() inside routes and REMOVE them.  
All backend DB writes MUST use supabaseAdmin.

────────────────────────────
3. FIX STRIPE WEBHOOK ROUTING ORDER
────────────────────────────

Open server.js or index.js.

ENSURE THIS ORDER:

// 1. Stripe webhook FIRST
app.post(
  "/api/webhooks/stripe",
  express.raw({ type: "application/json" }),
  stripeWebhookHandler
);

// 2. THEN enable JSON parsing
app.use(express.json());

// 3. THEN mount all other routes
app.use("/api", apiRouter);

If the webhook is mounted after express.json(), FIX IT.

────────────────────────────
4. FIX STRIPE WEBHOOK HANDLER
────────────────────────────

Open routes/stripeWebhook.js.

Ensure:

- Signature verification uses STRIPE_WEBHOOK_SECRET
- Event types handled:
    customer.subscription.created
    customer.subscription.updated
    customer.subscription.deleted
- On subscription active:
    Update profiles.membership_status = 'active'
- On cancellation:
    Update profiles.membership_status = 'inactive'

Ensure supabaseAdmin is used for updates.

Add console.log statements for debugging:
console.log("Stripe webhook received:", event.type);

────────────────────────────
5. FIX EVENT APPROVAL ROUTE
────────────────────────────

Open routes/adminApproval.js.

Ensure the route:

POST /api/admin/event-requests/:id/approve

Does the following:

1. Fetches the request
2. Creates an event row
3. Generates stream_key = crypto.randomUUID()
4. Generates stream_url = `rtmp://live.studioflow.tv/live/${stream_key}`
5. Inserts event slot with:
   - event_id
   - slot_title
   - upload_password
   - stream_key
   - stream_url
   - status = 'pending'
6. Updates custom_event_requests.status = 'approved'
7. Returns the event + slot

Ensure supabaseAdmin is used for all inserts.

────────────────────────────
6. FIX ROUTE MOUNTING
────────────────────────────

Open server.js and ensure:

app.use("/api/admin", adminRouter);
app.use("/api/live", liveEventsRouter);
app.use("/api/slot", recordedEventsRouter);

All routes must be mounted AFTER express.json()  
EXCEPT the Stripe webhook.

────────────────────────────
7. ADD ERROR LOGGING
────────────────────────────

Search all backend routes and add:

.catch(err => console.error("Backend error:", err));

Add console.log statements to confirm route execution.

────────────────────────────
8. VERIFY EVERYTHING WORKS
────────────────────────────

After fixes:

- Subscribing in Stripe updates membership_status in Supabase.
- Stripe webhook logs appear in Replit console.
- Approving an event request creates:
    event row
    event_slot row
    stream_key
    stream_url
- Approval modal closes without errors.
- Creator EventSlotView loads stream key and stream URL.

Make all changes now.
