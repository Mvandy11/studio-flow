You are re-running the full Studio Flow end‑to‑end user flow test now that:
- contest_entries, contest_votes, and free_tickets tables exist
- missing columns have been identified and will be patched
- the backend AI and upload endpoints are working

GOAL:
Simulate a real user journey:
1. Upload a file
2. Enhance it using /api/ai/enhance
3. Submit the enhanced file to a contest (this step will correctly return 401 without a real auth token)
4. Verify the contest entries feed via /api/contests/:id/entries

RULES:
- Do NOT modify any production code.
- Do NOT commit any test files.
- All tests must be temporary and run from the shell or a temporary script.

TASKS:

1. Create a temporary test script named:
   server/tests/fullFlowTest.js
   (Do NOT commit this file.)

2. In that script, perform the following steps:

   STEP A — Upload a test image
   - Download a small sample image (e.g. https://picsum.photos/400) into a Buffer.
   - Upload it to:
     POST ${process.env.API_BASE_URL}/api/upload
     using multipart/form-data.
   - Log the returned file URL.

   STEP B — Enhance the uploaded image
   - Send the uploaded file to:
     POST ${process.env.API_BASE_URL}/api/ai/enhance
     using multipart/form-data.
   - Log the enhanced image URL.

   STEP C — Submit the enhanced image to a contest
   - Fetch contests:
     GET ${process.env.API_BASE_URL}/api/contests
   - Select the first contest ID.
   - Attempt submission:
     POST ${process.env.API_BASE_URL}/api/contest-entries
     with JSON:
       {
         contest_id: "<id>",
         media_url: "<enhancedImageUrl>",
         title: "Automated Test Entry",
         description: "Full flow test"
       }
   - EXPECTED RESULT:
     401 Authentication required
     (This is correct — this endpoint requires a real user session.)

   STEP D — Verify the contest entries feed
   - Call:
     GET ${process.env.API_BASE_URL}/api/contests/<id>/entries
   - EXPECTED RESULT:
     200 OK with an empty array
     (After the schema patch is applied.)

3. Run the script using the backend environment so it uses the correct Supabase URL and keys.

4. After verification:
   - Delete server/tests/fullFlowTest.js
   - Leave no test artifacts behind.

5. Report:
   - Upload success
   - Enhance success
   - Submission (expected 401) success
   - Entries feed success (200 with empty array)
   - Any errors encountered
