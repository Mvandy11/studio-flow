You are updating the Studio Flow 2.0 codebase. Apply all of the following changes across the project:

────────────────────────────────────────
1. FIX ALL IMPORTS (GLOBAL)
────────────────────────────────────────
• Scan the entire project for incorrect relative imports.
• Fix all imports that incorrectly use default vs named exports.
• Fix all imports that use the wrong number of ../ segments.
• Ensure all hooks are imported as named exports unless the file explicitly exports default.
• Ensure all lib utilities are imported correctly from /src/lib.

────────────────────────────────────────
2. ADD MEMBERSHIP PLAN CONSTANTS
────────────────────────────────────────
Create a new file:

src/constants/memberships.js

Add the following:

export const MEMBERSHIP_PLANS = {
  member_30: {
    label: "Member",
    price: 30,
    rewardPool: 10,
    eventCreatorPool: 0,
  },
  creator_50: {
    label: "Creator Member",
    price: 50,
    rewardPool: 10,
    eventCreatorPool: 15,
  }
};

export function getPoolContributions(tier) {
  const plan = MEMBERSHIP_PLANS[tier];
  if (!plan) return { rewardPool: 0, eventCreatorPool: 0 };
  return {
    rewardPool: plan.rewardPool,
    eventCreatorPool: plan.eventCreatorPool
  };
}

────────────────────────────────────────
3. UPDATE MEMBERSHIP PAGE UI
────────────────────────────────────────
In src/pages/Membership.jsx (or wherever the membership page lives):

• Add a new UI block under the “Manage Subscription” button:

<div
  style={{
    marginTop: "0.75rem",
    padding: "0.75rem 1rem",
    background: "rgba(255,255,255,0.06)",
    borderRadius: "8px",
    fontSize: "0.85rem",
    lineHeight: "1.4",
    color: "rgba(255,255,255,0.85)",
    border: "1px solid rgba(255,255,255,0.08)"
  }}
>
  <strong style={{ color: "var(--accent-gold)" }}>
    Membership Management
  </strong>
  <p style={{ marginTop: "0.35rem" }}>
    Studio Flow uses secure Stripe Payment Links for memberships.
    To cancel your plan, upgrade to a new tier, or request end‑of‑service,
    contact support at:
  </p>

  <p
    style={{
      marginTop: "0.35rem",
      fontWeight: 600,
      color: "#fff",
      wordBreak: "break-all"
    }}
  >
    ObviouslyInspiredStudio@outlook.com
  </p>

  <p style={{ marginTop: "0.35rem", opacity: 0.8 }}>
    A team member will send your personalized Stripe link to complete the request.
  </p>
</div>

────────────────────────────────────────
4. ADD TIER COUNT HELPERS
────────────────────────────────────────
In the Membership page or Dashboard page:

• Add helper functions:

const member30Count = profiles.filter(p => p.membership_tier === "member_30").length;
const creator50Count = profiles.filter(p => p.membership_tier === "creator_50").length;

• Add a UI block:

<div className="membership-stats">
  <div className="stat"><h3>$30 Members</h3><p>{member30Count}</p></div>
  <div className="stat"><h3>$50 Creator Members</h3><p>{creator50Count}</p></div>
  <div className="stat"><h3>Total Members</h3><p>{member30Count + creator50Count}</p></div>
</div>

────────────────────────────────────────
5. ADD POOL CONTRIBUTION LOGIC (BACKEND)
────────────────────────────────────────
In any server route or Supabase function that handles membership activation or renewal:

• Import:

import { getPoolContributions } from '../constants/memberships';

• After confirming the membership tier:

const { rewardPool, eventCreatorPool } = getPoolContributions(tier);

• Insert into Supabase tables:

await supabase.from('reward_pool_contributions').insert({
  user_id: user.id,
  amount: rewardPool,
  tier
});

await supabase.from('event_creator_pool_contributions').insert({
  user_id: user.id,
  amount: eventCreatorPool,
  tier
});

────────────────────────────────────────
6. CLEAN UP MEMBERSHIP PAGE FEATURES LIST
────────────────────────────────────────
Ensure the $50 Creator Plan includes:

• Create/publish events
• RTMP/HLS streaming
• Donations
• AI tools (enhance/upscale/denoise)
• Creator Academy
• Contests
• Free Chat
• Early announcements
• Likes/support
• Priority review
• $10 → Reward Pool
• $15 → Event Creator Pool
• Creator Dashboard + Analytics
• Ticket sales (Stripe Payment Links)
• Premier Payout Settings (Stripe Connect)

Ensure the $30 Plan includes:

• Contests
• Free Chat
• Early announcements
• Likes/support
• $10 → Reward Pool

────────────────────────────────────────
7. ENSURE NO STRIPE SUBSCRIPTION CODE REMAINS
────────────────────────────────────────
• Remove or comment out any code referencing:
  - Stripe Billing Portal
  - subscription objects
  - portal sessions
  - invoice webhooks
  - customer.subscription events

Studio Flow 2.0 uses Payment Links only.

────────────────────────────────────────
8. FINAL CLEANUP
────────────────────────────────────────
• Remove unused imports.
• Fix all ESLint warnings.
• Ensure all components compile under Vite.
• Ensure Netlify build passes.
• Ensure no references to the Stripe Portal remain.
• Ensure membership tier logic is consistent across the app.

Apply all changes now.
