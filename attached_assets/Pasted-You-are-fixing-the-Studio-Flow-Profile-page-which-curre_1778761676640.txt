You are fixing the Studio Flow Profile page, which currently shows a black screen for non-admin users due to outdated template logic, missing membership integration, and missing fallback UI.

GOAL:
Replace the old profile page with a new, fully functional Profile + Membership page that uses the shared membership system and never renders a blank screen.

────────────────────────────────────────
1. REMOVE OLD PROFILE PAGE LOGIC
────────────────────────────────────────

Remove or replace any of the following from Studio Flow:

- Old template profile components
- Any references to:
  - user.subscription
  - user.plan
  - user.tier
  - user.profileInfo
  - user.billingInfo
- Any DreamPort-specific profile logic
- Any template Suspense wrappers with no fallback

These are causing the black screen.

────────────────────────────────────────
2. CREATE NEW PROFILE PAGE
────────────────────────────────────────

Create a new page:

/profile

This page must include:

SECTION: User Info
- Avatar
- Name
- Email
- Join date

SECTION: Membership
- Membership tier (Free / Monthly / Enterprise)
- Status (Active / Inactive)
- Renewal date (if available)
- Membership badge:
  - Free → gray
  - Monthly → blue
  - Enterprise → gold
- Button: “Upgrade Membership”
- Button: “Manage Subscription” (Stripe portal)

SECTION: Payout Settings
- Link to /settings/payouts
- Show current payout method (if any)
- Button: “Add Payout Method”

SECTION: Account Settings
- Change password (if email/password)
- Delete account (optional)
- Logout button

────────────────────────────────────────
3. ADD useMembership() HOOK
────────────────────────────────────────

Create a shared hook:

useMembership()

It must:
- Read the current user from Supabase auth
- Query the shared memberships table:
  memberships:
    user_id
    tier
    is_active
    started_at
    expires_at
    stripe_customer_id
    stripe_subscription_id
- Return:
  {
    tier,
    isActive,
    expiresAt,
    loading,
    error
  }

If no membership row exists:
- Return tier = "free"
- Return isActive = false

────────────────────────────────────────
4. ADD FALLBACK UI (NO MORE BLACK SCREEN)
────────────────────────────────────────

If membership fails to load:
Show:

“You are currently on the Free tier. Upgrade your membership to unlock more features.”

If user data fails to load:
Show:

“Unable to load profile. Please refresh.”

Never show a blank screen.

────────────────────────────────────────
5. UPDATE PROFILE MENU (TOP-RIGHT)
────────────────────────────────────────

Update the profile dropdown to include:

- My Profile
- My Membership
- Manage Subscription
- Billing
- Settings
- Logout

“My Membership” should link to /profile#membership.

────────────────────────────────────────
6. REMOVE SUBSCRIPTION FROM SIDEBAR
────────────────────────────────────────

Remove any sidebar items labeled:
- Subscription
- Membership
- Upgrade

Sidebar should only show platform features.

────────────────────────────────────────
7. ADD DONATION LINK TO SIDEBAR
────────────────────────────────────────

Add:
“Support Studio Flow”

Link:
https://buy.stripe.com/28E14pgpncgofnmbh3b7y0t

────────────────────────────────────────
8. FIX LAYOUT + SPACING
────────────────────────────────────────

Apply consistent layout rules:

- Page max-width: 1440px
- Outer padding: 24px
- Section spacing: 32px
- Card spacing: 16px
- Card padding: 20px
- Header height: 64px
- Profile menu aligned right with 16px margin

────────────────────────────────────────
9. CLEAN UP OLD CODE
────────────────────────────────────────

Search for and remove:
- Old profile components
- Old membership components
- Template leftovers
- Unused imports

────────────────────────────────────────
10. OUTPUT REQUIREMENTS
────────────────────────────────────────

At the end, print:

- All modified files
- All new files created
- All removed files
- All updated components
- Any new hooks created
- Any TODOs requiring manual Supabase or Stripe updates
