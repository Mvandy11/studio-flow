You are updating the Studio Flow frontend to match the new Supabase contest engine and remove all legacy contest logic.

GOAL:
Reset the entire contest UI so that contests are:
- never-ending
- always active
- ranked by like_count
- managed manually by admin
- powered ONLY by the new Supabase views:
    - contest_submission_feed
    - contest_leaderboard
    - admin_contest_dashboard

Remove ALL old contest logic from the codebase.

---------------------------------------
REMOVE ALL LEGACY CONTEST SYSTEM CODE
---------------------------------------

Search the entire codebase and DELETE or REWRITE anything referencing:

- contest_entries
- contest_votes
- free_tickets
- contest_prizes
- contest_results
- contest_participants
- old contest_winners table
- voting mode
- voting windows
- countdown timers
- start_date / end_date logic
- archive state
- locked state
- ticket gating
- ticket purchases
- “End Contest”
- “Archive Contest”
- “Completed”
- “Voting”
- “Entries: X”
- any UI that expects entry counts from old tables

These features NO LONGER EXIST.

---------------------------------------
REPLACE ALL DATA SOURCES WITH NEW VIEWS
---------------------------------------

Replace ALL Supabase queries with:

1. Contest feed:
   supabase.from('contest_submission_feed').select('*')

2. Leaderboard:
   supabase.from('contest_leaderboard').select('*')

3. Admin dashboard:
   supabase.from('admin_contest_dashboard').select('*')

4. Individual submission:
   supabase.from('submissions').select('*').eq('id', submissionId)

5. Likes:
   supabase.from('likes')

6. Comments:
   supabase.from('comments')

7. Winners:
   supabase.from('winners')

---------------------------------------
UPDATE ALL TYPESCRIPT TYPES
---------------------------------------

Remove old interfaces:
- ContestEntry
- ContestVote
- ContestState
- ContestArchiveState
- ContestVotingState
- Ticket types
- Old Winner types

Add or update:
- Submission
- Like
- Comment
- Winner
- LeaderboardEntry
- FeedEntry
- AdminContestDashboardEntry

Ensure all components use the new types.

---------------------------------------
UPDATE ALL UI COMPONENTS
---------------------------------------

1. Contest List Page:
   - Remove “Voting”, “Completed”, “Archived”
   - Remove “End Contest”, “Archive”
   - Remove entry counts from old tables
   - Show:
       - Title
       - Active (always true)
       - Submission count (from admin_contest_dashboard)
       - Total likes (from admin_contest_dashboard)
       - Winner preview (if exists)

2. Contest Detail Page:
   - Remove countdowns
   - Remove voting state
   - Remove ticket gating
   - Remove entry buttons tied to old tables
   - Show submissions sorted by like_count

3. Leaderboard Page:
   - Use contest_leaderboard
   - Sort by like_count DESC

4. Submission Feed:
   - Use contest_submission_feed
   - Show like_count, comment_count, is_liked_by_me

5. Admin Dashboard:
   - Use admin_contest_dashboard
   - Show:
       - submission_count
       - total_like_count
       - winner preview
   - Add:
       - “Select Winner” button (manual)
       - “Feature Winner” button
       - “Payout Winner” button
   - Remove:
       - End Contest
       - Archive
       - Voting state

---------------------------------------
CLEANUP & VALIDATION
---------------------------------------

- Remove all unused imports
- Remove all unused components
- Remove all unused hooks
- Remove all unused services
- Ensure no file references old contest tables
- Ensure no file references voting or archive logic
- Ensure no file references countdown timers
- Ensure no file references ticket logic

---------------------------------------
FINAL REQUIREMENTS
---------------------------------------

- App must compile with ZERO TypeScript errors
- App must build successfully
- Print a list of all modified files
- Print a list of all removed legacy logic
- Print a list of all updated queries
- Print any remaining references that require manual review

BEGIN NOW.
