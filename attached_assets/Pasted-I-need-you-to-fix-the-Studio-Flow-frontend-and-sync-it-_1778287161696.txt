I need you to fix the Studio Flow frontend and sync it with Supabase so that contests, events, and categories behave consistently across the dashboard and dropdown. Follow these steps exactly and only modify the files required.

1. FIX DROPDOWN CONTESTS (BROKEN LINKS)
A. Remove all hard‑coded contest links
Find the dropdown menu component (Navbar, Sidebar, or Header).

Delete any static contest entries like:

jsx
<Link to="/contests/123">...</Link>
B. Fetch contests dynamically from Supabase
Add:

js
const { data: contests } = await supabase
  .from("contests")
  .select("id, title")
  .eq("status", "active");
C. Render dropdown contests dynamically
Replace the old contest list with:

jsx
{contests?.map(contest => (
  <DropdownItem key={contest.id} to={`/contests/${contest.id}`}>
    {contest.title}
  </DropdownItem>
))}
D. Ensure correct routing
The dropdown must use:

Code
/contests
/contests/:contestId
2. REMOVE OLD EVENTS SECTION FROM DASHBOARD
Find the dashboard component (Dashboard.jsx or Home.jsx).

Delete the entire static “Events” section including:

Creator Showcase Night

Beat Battle Live

Comedy Open Mic

Art & Design Expo

Producer Workshop

Freestyle Friday

These are outdated and not connected to Supabase.

Remove any old event components or imports.

3. CREATE REAL EVENTS TABLE IN SUPABASE
Create a new table named events with the following columns:

Code
id: uuid (primary key)
title: text
description: text
price: numeric
event_type: text
start_time: timestamp
location: text
image_url: text
created_at: timestamp (default now())
Do NOT modify existing tables.

4. REBUILD EVENTS BACKEND (ESM)
Create a new file:

Code
server/routes/events.js
With routes:

GET /events
Return all events.

GET /events/:id
Return a single event.

POST /events (admin only)
Insert a new event.

DELETE /events/:id (admin only)
Delete an event.

Mount in server/app.js:

js
import eventsRouter from "./routes/events.js";
app.use("/api/events", eventsRouter);
5. REBUILD EVENTS FRONTEND
Create a new page:

Code
src/pages/Events.jsx
Fetch events from Supabase:

js
const { data: events } = await supabase
  .from("events")
  .select("*")
  .order("start_time", { ascending: true });
Render event cards dynamically.

Create event details page:

Code
src/pages/EventDetails.jsx
Add routing:

jsx
<Route path="/events" element={<EventsPage />} />
<Route path="/events/:eventId" element={<EventDetailsPage />} />
6. SYNC DASHBOARD + DROPDOWN CATEGORIES
Replace all category lists with:

js
export const categories = [
  {
    id: "custom-events",
    title: "Custom Events",
    route: "/custom-event-request"
  },
  {
    id: "contests",
    title: "Contests",
    route: "/contests"
  },
  {
    id: "submissions",
    title: "Submissions",
    route: "/submissions"
  }
];
Remove:

Events

Education

DIY

Event Creator

Any old categories

Update dashboard to import and map over this list.

7. VERIFY CONTEST VIDEO SUBMISSION FLOW
Ensure:

/contests loads the contest list

/contests/:contestId loads contest details

“Submit Entry” button works

Video upload still inserts into contest_entries

No broken routes remain

Do NOT modify backend contest logic — it already works.

END PROMPT