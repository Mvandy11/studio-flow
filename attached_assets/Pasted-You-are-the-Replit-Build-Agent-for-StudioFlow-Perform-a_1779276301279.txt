You are the Replit Build Agent for StudioFlow. Perform a full cleanup and repair pass focused on chat, events, contests, and Stripe membership integration. Do NOT modify working features such as free chat, video uploads, or contest likes.

====================================================
1. REMOVE LIVE CHAT MODULE COMPLETELY
====================================================
Live Chat is a duplicate of Free Chat and must be removed.

Tasks:
• Delete /src/modules/livechat entirely.
• Remove all imports, routes, navigation links, and references to live chat.
• Ensure only the Free Chat module remains active.
• Verify no components still reference "live" or "livechat".

====================================================
2. FIX EVENTS MODULE (Requests Not Showing)
====================================================
Symptoms: Event requests are being submitted but do not appear in the UI.

Tasks:
• Verify the frontend is querying the correct table: "events".
• Ensure the insert uses the correct fields: id, created_by, title, description, date, time, status.
• Fix any mismatched field names (e.g., "message" vs "content", "request" vs "event").
• Check RLS: users must be able to SELECT events they created.
• Ensure the list view is not filtering out events by incorrect status.
• Add console logging around event fetch to confirm data is returned.

====================================================
3. FIX CONTEST COMMENTS (Likes Work, Comments Don’t)
====================================================
Symptoms: Likes work, comments silently fail.

Tasks:
• Verify the correct table name: "contest_comments".
• Ensure the insert uses the correct fields: id, entry_id, user_id, content, created_at.
• Replace any remaining "message" fields with "content".
• Fix any API route mismatches in /netlify/functions or /api.
• Ensure RLS allows inserting comments for authenticated users.
• Ensure the comment list view is reading from the correct table.

====================================================
4. STRIPE MEMBERSHIP INTEGRATION CHECK
====================================================
Do NOT change working checkout logic. Only verify the following:

Tasks:
• Confirm POST /api/payments/create-checkout-session includes metadata.user_id.
• Confirm the webhook handler updates profiles.subscription_active = true.
• Confirm no old "legacy" subscription routes remain.
• Confirm the frontend calls supabase.auth.refreshSession() after returning from Stripe.
• Confirm the MembershipGate component reads subscription_active correctly.

====================================================
5. CLEAN UP MESSAGE/CONTENT FIELD MISMATCHES
====================================================
StudioFlow previously used "message" but now uses "content".

Tasks:
• Search the entire repo for "message" fields in chat, contests, events, and comments.
• Replace with "content" where appropriate.
• Ensure all Supabase inserts and selects use the correct field names.

====================================================
6. GENERAL CLEANUP
====================================================
• Remove unused imports, dead code, and deprecated components.
• Ensure all modules compile without warnings.
• Output a summary of:
  - Files modified
  - Modules cleaned
  - Schema mismatches fixed
  - Any remaining issues detected

End of directive.
