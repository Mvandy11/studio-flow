You are updating Studio Flow and DreamPort with three major new features:

1. Premier Payout System (Venmo, PayPal, Stripe Connect, CashApp)
2. Automatic Name Picker + Drawing Pot System for ticketed events
3. New Studio Flow donation link

This task updates backend schema, frontend UI, and server logic.

────────────────────────────────────────
1. UPDATE STUDIO FLOW DONATION LINK
────────────────────────────────────────

Replace ALL existing Studio Flow donation URLs with:

https://buy.stripe.com/28E14pgpncgofnmbh3b7y0t

Update:
- Dashboard donation button
- Sidebar donation link
- Footer donation link
- Any “Support Studio Flow” modules

No backend changes required.

────────────────────────────────────────
2. PREMIER PAYOUT SYSTEM (CREATOR PAYOUTS)
────────────────────────────────────────

Goal:
Creators can choose their payout method:
- PayPal
- Venmo
- Stripe Connect
- CashApp
(Ko‑Fi must be removed entirely)

Add a new table:

payout_methods:
  id uuid primary key
  user_id uuid references auth.users
  method text        -- 'paypal', 'venmo', 'stripe', 'cashapp', 'bank'
  account text       -- email, phone, handle, cashtag, or account ID
  created_at timestamp default now()
  updated_at timestamp default now()

Add a new table for payout logs:

payout_logs:
  id uuid primary key
  user_id uuid
  event_id uuid
  amount numeric
  method text
  status text
  created_at timestamp default now()

────────────────────────────────────────
3. CREATOR PAYOUT SETTINGS PAGE
────────────────────────────────────────

Add a new page in BOTH Studio Flow and DreamPort:

/settings/payouts

UI requirements:
- Dropdown: Select payout method
- Input: Account info
- Save button
- Validation:
  - PayPal → email
  - Venmo → @handle or phone
  - Stripe → connect account ID
  - CashApp → $cashtag

Store results in payout_methods table.

────────────────────────────────────────
4. AUTOMATED PAYOUT FLOW
────────────────────────────────────────

When an event or contest ends:

1. Calculate creator earnings
2. Read creator’s payout method from payout_methods
3. Trigger payout via:
   - PayPal Payouts API
   - Venmo Business API
   - Stripe Connect Transfers
   - CashApp API (optional later)
4. Log payout in payout_logs

Add server routes:
- POST /payouts/initiate
- POST /payouts/complete
- GET /payouts/history

────────────────────────────────────────
5. DRAWING POT SYSTEM FOR EVENTS
────────────────────────────────────────

Creators can enable a drawing for any event.

Add fields to events table:

ALTER TABLE events
ADD COLUMN IF NOT EXISTS drawing_enabled boolean default false;

ALTER TABLE events
ADD COLUMN IF NOT EXISTS drawing_amount numeric;

Example:
Ticket price = $10
drawing_amount = $5
→ $5 goes into the drawing pot
→ Every ticket = 1 entry

────────────────────────────────────────
6. TICKET PURCHASES TABLE
────────────────────────────────────────

Add:

ticket_purchases:
  id uuid primary key
  event_id uuid
  user_id uuid
  ticket_price numeric
  drawing_entry boolean default false
  created_at timestamp default now()

Every ticket purchase inserts a row.

If drawing_enabled = true:
- drawing_entry = true

────────────────────────────────────────
7. DRAWING POT CALCULATION
────────────────────────────────────────

Total pot = COUNT(drawing_entry=true) * drawing_amount

Add this to event detail page:
- Total Pot
- Total Entries

────────────────────────────────────────
8. AUTOMATIC NAME PICKER (ADMIN ONLY)
────────────────────────────────────────

Add a button on event admin page:

“Pick Random Winner”

SQL:

SELECT user_id
FROM ticket_purchases
WHERE event_id = :event_id AND drawing_entry = true
ORDER BY RANDOM()
LIMIT 1;

Return:
- Winner name
- Winner email
- Total pot
- Button: “Send Payout”

────────────────────────────────────────
9. ADMIN PAYOUT FOR DRAWING WINNER
────────────────────────────────────────

When admin clicks “Send Payout”:
- Use winner’s payout method (if available)
- If not available, show “Winner must add payout method”
- Trigger payout via same APIs as creator payouts
- Log in payout_logs

────────────────────────────────────────
10. FRONTEND UPDATES
────────────────────────────────────────

Studio Flow + DreamPort:

- Add “Drawing Settings” to event creation:
  - Toggle: Enable Drawing
  - Input: drawing_amount

- Add “Drawing Pot” display on event page
- Add “Pick Winner” button on admin page
- Add “Winner Modal” with:
  - Winner name
  - Winner email
  - Total pot
  - Send payout button

- Add “Payout Settings” page under user settings

────────────────────────────────────────
11. REMOVE KO-FI COMPLETELY
────────────────────────────────────────

Search entire codebase for:
- ko-fi
- kofi
- kofipay

Remove all references.

────────────────────────────────────────
12. OUTPUT REQUIREMENTS
────────────────────────────────────────

At the end, print:

- All modified files
- All new files created
- All new components
- All new server routes
- All new Supabase schema changes
- Any TODOs requiring manual SQL
