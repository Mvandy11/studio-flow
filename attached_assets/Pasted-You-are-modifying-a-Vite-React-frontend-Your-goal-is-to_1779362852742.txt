You are modifying a Vite + React frontend.  
Your goal is to eliminate Supabase auth duplication issues that cause:

- "Multiple GoTrueClient instances detected"
- "Lock was not released within 5000ms"
- "Another request stole it"
- UI crashes like "Cannot read properties of undefined (reading 'border')"
- Flickering login state
- Unstable membership state

These errors occur when more than one Supabase client is created in the browser.

────────────────────────────────────────
TASKS — DO ALL OF THE FOLLOWING
────────────────────────────────────────

1. SEARCH THE ENTIRE PROJECT for any Supabase client files:
   Look for files containing createClient() or supabase initialization:
   - src/lib/supabaseClient.ts
   - src/lib/supabaseClient.js
   - src/lib/supabaseClient.jsx
   - src/lib/supabaseClient.mjs
   - src/supabaseClient.js
   - src/supabase.js
   - src/lib/supabase.ts
   - ANY file that calls createClient()

2. ENSURE ONLY ONE CLIENT FILE EXISTS:
   Keep ONLY this file:
     src/lib/supabaseClient.ts

   Delete ALL other Supabase client files.

3. REWRITE src/lib/supabaseClient.ts to EXACTLY this:

   import { createClient } from "@supabase/supabase-js";

   const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
   const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;

   export const supabase = createClient(supabaseUrl, supabaseAnonKey);

   export default supabase;

4. UPDATE ALL IMPORTS across the entire frontend:
   Replace ANY import of supabase from ANY other path with:

     import supabase from "../lib/supabaseClient";
     OR
     import { supabase } from "../lib/supabaseClient";

   Ensure ALL imports point to the SAME file:
     src/lib/supabaseClient.ts

5. ENSURE NO COMPONENT, HOOK, OR UTILITY creates its own Supabase client.
   Remove any createClient() calls outside supabaseClient.ts.

6. CLEAN UP VITE HOT RELOAD ARTIFACTS:
   - Remove any duplicate compiled files if present.
   - Ensure no stale .js version of the client remains.

7. VERIFY:
   - The project has exactly ONE Supabase client instance.
   - All imports reference src/lib/supabaseClient.ts.
   - No duplicate clients are created.

This will eliminate all GoTrue lock errors, stop the UI crashes, and stabilize login + membership state.

Make all changes now.
