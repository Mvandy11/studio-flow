You are modifying the Studio Flow backend and frontend to add a complete Contest Comment system. This must NOT break any existing Supabase free‑chat or contest features. Follow these steps exactly.

────────────────────────────
DATABASE (Supabase)
────────────────────────────

1. Create a new table: contest_comments
   Columns:
     - id: uuid, primary key, default uuid_generate_v4()
     - contest_id: uuid, references contests(id)
     - user_id: uuid, references profiles(id)
     - content: text
     - created_at: timestamp with time zone, default now()

2. Enable RLS on contest_comments.

3. Add policies:
   -- Allow authenticated users to insert
   create policy "allow insert" on contest_comments
   for insert
   to authenticated
   with check (auth.uid() = user_id);

   -- Allow public read
   create policy "allow read" on contest_comments
   for select
   to public
   using (true);

────────────────────────────
BACKEND (Node / Express)
────────────────────────────

4. Create a new backend route file:
   routes/contestComments.js

5. Add two routes:

   GET /api/contests/:id/comments
     - Fetch all comments for that contest
     - Order by created_at ascending
     - Return full comment list

   POST /api/contests/:id/comments
     - Validate user_id, content
     - Insert comment using supabaseAdmin
     - Return inserted row

6. Ensure the route uses:
   import { supabaseAdmin } from "../supabase/supabaseAdmin.js";

7. Mount the route in app.js AFTER express.json():
   app.use("/api/contests", contestCommentsRouter);

────────────────────────────
FRONTEND (React)
────────────────────────────

8. In the contest detail page, add:
   - A comment list component
   - A comment input box
   - A submit button

9. Fetch comments from:
   GET /api/contests/${contestId}/comments

10. Post comments to:
   POST /api/contests/${contestId}/comments

11. Ensure the request body includes:
   {
     user_id: user.id,
     content: commentText
   }

12. After posting, reload the comment list.

13. DO NOT call Supabase REST directly.
    Remove any fetches to:
    /rest/v1/contest_comments

────────────────────────────
VERIFICATION
────────────────────────────

14. Confirm:
   - Comments load under each contest
   - Comments post successfully
   - No 404 errors
   - No RLS errors
   - No Supabase REST calls
   - No interference with free‑chat

Make all changes now.
