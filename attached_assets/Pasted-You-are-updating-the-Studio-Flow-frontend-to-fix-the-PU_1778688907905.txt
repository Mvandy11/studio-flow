You are updating the Studio Flow frontend to fix the PUBLIC Contest View so customers can browse contests, open a contest, and submit videos/photos without admin permissions.

GOAL:
Ensure the public-facing Contests page and Contest Detail page use ONLY public-safe Supabase views and NEVER call admin-only functions such as is_admin().

---------------------------------------
STEP 1 — FIX PUBLIC CONTEST LIST DATA SOURCE
---------------------------------------

Search the entire codebase for any public-facing query using:

- admin_contest_dashboard
- is_admin()
- winners table directly
- submissions table directly
- any old contest tables (contest_entries, contest_votes, etc.)

Replace ALL public contest list queries with:

supabase.from('contests').select('*')

OR, if the UI needs ranking info:

supabase.from('contest_leaderboard')
        .select('*')
        .order('like_count', { ascending: false })

These views are safe for anon/public users.

---------------------------------------
STEP 2 — REMOVE ALL CALLS TO is_admin() FROM PUBLIC ROUTES
---------------------------------------

Search for any usage of:

- is_admin()
- admin_contest_dashboard
- admin-only fields (submission_count, total_like_count, winner preview)

Remove these from ALL public pages.

These are admin-only and will ALWAYS throw RLS errors for anon users.

---------------------------------------
STEP 3 — FIX CONTEST DETAIL PAGE (PUBLIC)
---------------------------------------

Replace any old queries with:

1. Contest metadata:
   supabase.from('contests')
           .select('*')
           .eq('id', contestId)
           .single()

2. Submissions for this contest:
   supabase.from('contest_submission_feed')
           .select('*')
           .eq('contest_id', contestId)
           .order('like_count', { ascending: false })

3. Likes:
   supabase.from('likes')

4. Comments:
   supabase.from('comments')

Do NOT use admin_contest_dashboard on public pages.

---------------------------------------
STEP 4 — ENABLE PUBLIC SUBMISSION CREATION
---------------------------------------

Ensure the “Submit Your Video” or “Upload Submission” button uses:

supabase.from('submissions').insert({
  contest_id,
  user_id,
  user_name,
  user_email,
  media_url,
  description,
  status: 'active'
})

Make sure RLS allows:
- authenticated users to insert submissions
- anon users CANNOT insert submissions

---------------------------------------
STEP 5 — FIX PUBLIC UI EXPECTATIONS
---------------------------------------

Remove any UI elements expecting:
- submission_count (admin-only)
- total_like_count (admin-only)
- winner preview (admin-only)
- voting state
- archive state
- countdown timers
- ticket gating
- “End Contest”
- “Archive Contest”

Public UI should show:
- contest title
- contest category
- contest description
- submissions sorted by like_count
- like buttons
- comment buttons
- upload button (if logged in)

---------------------------------------
STEP 6 — CLEANUP & VALIDATION
---------------------------------------

- Remove unused imports
- Remove unused types tied to old contest logic
- Remove unused components
- Ensure no public page references admin-only views
- Ensure the app compiles with zero TypeScript errors

---------------------------------------
FINAL OUTPUT
---------------------------------------

After completing the work, print:
- All modified files
- All updated Supabase queries
- All removed admin-only logic from public pages
- Any remaining issues requiring manual review
