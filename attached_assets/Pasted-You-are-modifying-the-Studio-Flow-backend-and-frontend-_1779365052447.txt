You are modifying the Studio Flow backend and frontend to fix the video comment system. Comments currently fail silently. Fix all issues so comments can be posted and retrieved reliably.

────────────────────────────
BACKEND FIXES
────────────────────────────

1. Open routes/freeChat.js (or the comment route file).
2. Ensure the route is mounted AFTER express.json() in app.js.
3. Ensure the route path matches the frontend:
   - If frontend posts to /api/comments, rename the route accordingly.
   - If backend uses /api/free-chat, update the frontend to match.

4. Rewrite the POST handler to:
   - Validate req.body.user_id, req.body.video_id, req.body.content
   - Use supabaseAdmin (NOT supabase) to insert the comment
   - Return the inserted row
   - Never crash the server

Example:

router.post("/", async (req, res) => {
  const { user_id, video_id, content } = req.body;
  if (!user_id || !video_id || !content) {
    return res.status(400).json({ error: "Missing fields" });
  }

  const { data, error } = await supabaseAdmin
    .from("comments")
    .insert({ user_id, video_id, content })
    .select()
    .single();

  if (error) return res.status(400).json({ error: error.message });
  res.json({ comment: data });
});

5. Add a GET route:
   GET /api/comments/:video_id
   - Return all comments for that video ordered by created_at.

────────────────────────────
FRONTEND FIXES
────────────────────────────

6. Open the video page component.
7. Ensure it POSTs to the correct backend route:
   fetch(`${API_BASE}/api/comments`, { ... })

8. Ensure the request body includes:
   - user.id
   - video.id
   - comment text

9. Ensure comments reload after posting.

────────────────────────────
SUPABASE RLS FIXES
────────────────────────────

10. Open the Supabase SQL editor.
11. Ensure the comments table has these policies:

-- Allow authenticated users to insert comments
create policy "allow insert" on comments
for insert
to authenticated
with check (auth.uid() = user_id);

-- Allow anyone to read comments
create policy "allow read" on comments
for select
to public
using (true);

12. Ensure no old or conflicting policies exist.

────────────────────────────
FINAL CHECK
────────────────────────────

13. Test posting a comment.
14. Test retrieving comments.
15. Ensure no 401, 403, or RLS errors.
16. Ensure the UI updates immediately.

Make all changes now.
