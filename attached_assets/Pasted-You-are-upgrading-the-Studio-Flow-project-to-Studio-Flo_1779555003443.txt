You are upgrading the Studio Flow project to "Studio Flow 2.0".

The goals:

- Use Stripe Payment Links (not Subscriptions) for all billing
- Implement clean membership tiers: free, $30 member, $50 creator
- Replace event requests with category-based event slots
- Add donation tracking per event and per creator
- Implement a creator revenue pool ($25 from each creator_50 + donations)
- Clean up Supabase schema and frontend access logic
- Remove all Stripe subscription webhook complexity

Make the following changes step by step.

────────────────────────────────────────
1. STRIPE + MEMBERSHIP MODEL (PAYMENT LINKS ONLY)
────────────────────────────────────────

Studio Flow uses these Payment Links:

$30 Member Tier:
  URL: https://buy.stripe.com/7sYdRb2yx3JS1wwacZb7y0o
  Success URL: https://studioflow.tv/membership/success?tier=member_30

$50 Creator Tier:
  URL: https://buy.stripe.com/cNi6oJ1utcgo3EE4SFb7y0u
  Success URL: https://studioflow.tv/membership/success?tier=creator_50

Donation Link:
  URL: https://buy.stripe.com/28E14pgpncgofnmbh3b7y0t
  Success URL: https://studioflow.tv/donate/success?event_id={EVENT_ID}

Ensure these URLs are reflected in the frontend where appropriate.

────────────────────────────────────────
2. REMOVE STRIPE SUBSCRIPTION / WEBHOOK LOGIC
────────────────────────────────────────

Delete or disable any code that:

- Uses Stripe Subscriptions API
- Handles events like:
  - customer.subscription.created
  - customer.subscription.updated
  - invoice.paid
  - invoice.payment_failed
- Syncs subscription status to Supabase
- Stores subscription IDs or metadata

Studio Flow 2.0 uses Payment Links only. No subscription objects.

────────────────────────────────────────
3. SUPABASE SCHEMA — PROFILES & MEMBERSHIP
────────────────────────────────────────

Ensure the profiles table has at least:

profiles:
  id uuid primary key
  email text
  membership_active boolean default false
  membership_tier text default 'free'  -- 'free' | 'member_30' | 'creator_50'
  membership_started_at timestamp null
  created_at timestamp default now()

Add or alter columns as needed.

────────────────────────────────────────
4. MEMBERSHIP ACTIVATION ENDPOINT
────────────────────────────────────────

Create a backend route or serverless function:

POST /api/membership/activate

Behavior:

- Read JSON body: { tier: "member_30" | "creator_50" | null }
- Read the authenticated Supabase user (session)
- If no user, return 401

If tier === "member_30":
  membership_active = true
  membership_tier = "member_30"
  membership_started_at = now()

If tier === "creator_50":
  membership_active = true
  membership_tier = "creator_50"
  membership_started_at = now()

If tier is null or missing:
  membership_active = false
  membership_tier = "free"

Return JSON: { success: true }

────────────────────────────────────────
5. MEMBERSHIP SUCCESS PAGE
────────────────────────────────────────

Create or update page:

/membership/success

Logic:

- Read tier from URL:
    const tier = new URLSearchParams(window.location.search).get("tier")

- Call:
    await fetch("/api/membership/activate", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ tier })
    })

- After success, redirect user to:
    - Creator dashboard if tier === "creator_50"
    - Member home if tier === "member_30"
    - General home otherwise

────────────────────────────────────────
6. EVENT SLOT SYSTEM (CATEGORY-BASED EVENTS)
────────────────────────────────────────

Create a Supabase table:

event_slots:
  id uuid primary key default uuid_generate_v4()
  creator_id uuid references profiles(id)
  title text
  description text
  category text
  thumbnail_url text
  video_url text
  is_live boolean default false
  created_at timestamp default now()
  updated_at timestamp default now()

Categories are simple text values, e.g.:
"Comedy", "Music", "Dance", "Fitness", "Gaming", "Education",
"Cooking", "Motivation", "Kids", "Talk Show", "Tutorials", "Art".

Remove any old "event request" tables and approval logic.

────────────────────────────────────────
7. EVENT CREATION UI (CREATOR-ONLY)
────────────────────────────────────────

Add a page:

/creator/new-event

Requirements:

- Only accessible if membership_tier === "creator_50"
- Form fields:
  - Title
  - Description
  - Category (dropdown from fixed list)
  - Thumbnail upload
  - Video upload OR "Go Live" toggle

On submit:

- Insert into event_slots with creator_id = current user.

────────────────────────────────────────
8. EVENT LISTING & VIEWING
────────────────────────────────────────

Add:

/events
  - List all event_slots
  - Filter by category via query param or UI

/events/:category
  - List event_slots where category = :category

/event/:id
  - Show single event:
    - Title
    - Description
    - Category
    - Creator info
    - Video or live player
    - Donation button (see section 9)
    - Chat and reactions (reuse existing components if present)

────────────────────────────────────────
9. DONATION TRACKING
────────────────────────────────────────

Create a Supabase table:

donations:
  id uuid primary key default uuid_generate_v4()
  event_id uuid references event_slots(id)
  creator_id uuid references profiles(id)
  user_id uuid references profiles(id)
  amount numeric
  created_at timestamp default now()

On each event page (/event/:id):

- Show a "Support This Event" button that links to:
  https://buy.stripe.com/28E14pgpncgofnmbh3b7y0t
- Configure Stripe success URL as:
  https://studioflow.tv/donate/success?event_id={EVENT_ID}

Create /donate/success page:

- Read event_id from URL
- Read current user session
- Fetch event to get creator_id
- Insert into donations:
    event_id
    creator_id
    user_id
    amount (use fixed amount if Payment Link is fixed)
- Show a "Thank you" message and link back to the event.

On /event/:id:

- Query donations where event_id = id
- Show:
  - Total donated amount
  - Number of supporters

────────────────────────────────────────
10. CREATOR REVENUE POOL
────────────────────────────────────────

Create tables:

revenue_pool:
  id uuid primary key default uuid_generate_v4()
  month text  -- e.g. "2026-05"
  total_amount numeric default 0
  created_at timestamp default now()

revenue_pool_entries:
  id uuid primary key default uuid_generate_v4()
  creator_id uuid references profiles(id)
  amount numeric
  source text  -- "subscription" or "donation"
  created_at timestamp default now()

When membership_tier is set to "creator_50" in /api/membership/activate:

- Insert into revenue_pool_entries:
    creator_id: current user id
    amount: 25
    source: "subscription"

When a donation is recorded:

- Insert into revenue_pool_entries:
    creator_id: event.creator_id
    amount: donation.amount
    source: "donation"

Add a backend job or route to compute monthly totals:

- For a given month (e.g. "YYYY-MM"):
  - Sum all revenue_pool_entries.amount for that month
  - Insert or update revenue_pool with:
      month
      total_amount

────────────────────────────────────────
11. CREATOR DASHBOARD
────────────────────────────────────────

Update /creator/dashboard (or equivalent) to show:

- "My Events"
  - List event_slots where creator_id = current user

- "My Donations Received"
  - Sum donations where creator_id = current user
  - Show recent donations

- "My Revenue Pool Earnings"
  - Sum revenue_pool_entries where creator_id = current user
  - Group by month if needed

Only accessible if membership_tier === "creator_50".

────────────────────────────────────────
12. FRONTEND ACCESS LOGIC (TIERS)
────────────────────────────────────────

Everywhere you gate features, use:

If membership_tier === "creator_50":
  - Full access: create events, view events, donations, dashboard

If membership_tier === "member_30":
  - View events, chat, community
  - No event creation

If membership_tier === "free":
  - Browse public pages
  - Prompt to upgrade for events and creator tools

Ensure the Supabase client is initialized with:

auth: {
  persistSession: true,
  autoRefreshToken: true,
  detectSessionInUrl: true
}

And that you always call supabase.auth.getSession() before fetching profile data.

────────────────────────────────────────
13. CLEANUP & TESTING
────────────────────────────────────────

Remove:

- Any unused subscription tables
- Any Stripe subscription API calls
- Any webhook retry logic
- Any "event request" tables and UI

Test:

- Free user → membership_tier = "free"
- $30 payment → redirected to success → membership_tier = "member_30"
- $50 payment → redirected to success → membership_tier = "creator_50"
- Creator_50 can create events
- Member_30 can view events but not create
- Donations record correctly
- Revenue pool entries record correctly
- Creator dashboard shows events, donations, and earnings
- No Stripe webhook errors
- No subscription errors
- No 401/404 on membership/profile fetch

Make all changes now.
