You are testing the full Studio Flow user flow end‑to‑end using the live backend on Render and the live frontend environment.

GOAL:
Simulate a real user journey:
1. Upload a file
2. Enhance it using /api/ai/enhance
3. Submit the enhanced file to a contest
4. Verify the entry appears in the contest feed

RULES:
- Do NOT modify any production code.
- Do NOT commit any test files.
- All tests must be temporary and run from the shell or a temporary script.

TASKS:

1. Create a temporary test script named:
   src/tests/fullFlowTest.js
   (Do NOT commit this file.)

2. In the script, perform the following steps:

   STEP A — Upload a test image
   - Use a small sample image from a public URL (e.g. https://picsum.photos/400)
   - Download it to a Buffer
   - Upload it to the backend using:
     POST ${import.meta.env.VITE_API_BASE_URL}/api/upload
     with multipart/form-data
   - Log the returned file URL

   STEP B — Enhance the uploaded image
   - Send the uploaded file to:
     POST ${import.meta.env.VITE_API_BASE_URL}/api/ai/enhance
     using multipart/form-data
   - Log the enhanced image URL

   STEP C — Submit the enhanced image to a contest
   - Choose the first contest returned by:
     GET ${import.meta.env.VITE_API_BASE_URL}/api/contests
   - Submit using:
     POST ${import.meta.env.VITE_API_BASE_URL}/api/contest-entries
     with JSON:
       {
         contest_id: "<id>",
         media_url: "<enhancedImageUrl>",
         title: "Test Entry",
         description: "Automated test submission"
       }
   - Log the returned entry ID

   STEP D — Verify the entry appears in the contest feed
   - Call:
     GET ${import.meta.env.VITE_API_BASE_URL}/api/contest-entries?contest_id=<id>
   - Confirm the new entry is present in the returned list

3. Run the script using the frontend dev environment so it uses the correct .env values.

4. After verification:
   - Delete src/tests/fullFlowTest.js
   - Leave no test artifacts behind.

5. Report:
   - Upload success
   - Enhance success
   - Submission success
   - Entry visibility success
   - Any errors encountered
