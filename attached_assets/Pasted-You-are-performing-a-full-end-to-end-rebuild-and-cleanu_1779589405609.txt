You are performing a full, end-to-end rebuild and cleanup of the Studio Flow
2.0 codebase. The project uses React + Vite + React Router, NOT Next.js.
Routing is handled in App.jsx. Stripe and ffmpeg MUST remain because they
power working features. Your goal is to deliver a fully functional,
launch-ready platform with a clean, feature-first architecture.

You must also validate and preserve the user’s recent manual changes:
- src/features/events/pages/NewEvent.jsx (or /creator/new-event.jsx)
- src/features/events/pages/MyEvents.jsx
- src/features/events/pages/EventDetail.jsx (event/[id].jsx equivalent)

Fix any issues in these files and integrate them into the new structure.

────────────────────────────────────────
0. VALIDATE EXISTING MANUAL CHANGES
────────────────────────────────────────

Before modifying anything:

- Locate the user’s manually created files for:
  - Create Event
  - My Events
  - Event Detail
- Ensure they compile
- Ensure imports are correct
- Ensure Supabase client is referenced correctly
- Ensure React Router navigation works
- Ensure no duplicate or conflicting versions exist

If issues exist, fix them cleanly.

────────────────────────────────────────
1. REORGANIZE INTO FEATURE-FIRST ARCHITECTURE
────────────────────────────────────────

Restructure the project into:

src/
  features/
    membership/
      pages/
      components/
      api/
    events/
      pages/
      components/
      api/
    creator/
      pages/
      components/
      api/
    contests/
      pages/
      components/
      api/
    donations/
      pages/
      components/
      api/
  shared/
    components/
    hooks/
    utils/
    styles/
  App.jsx
  main.jsx
  supabaseClient.js

Move all existing pages into the correct feature folders.

Remove:
- Old event request pages
- Old creator application pages
- Old membership pages
- Old contest pages
- Any unused components
- Any dead code

────────────────────────────────────────
2. FRONTEND IMPLEMENTATION (React Router)
────────────────────────────────────────

Update App.jsx routing to:

/membership
/membership/success
/events
/events/:category
/event/:id
/creator/dashboard
/creator/new-event
/creator/events
/creator/donations
/creator/revenue
/contests
/contests/my-entries
/donate/success

Implement or fix the following pages:

A) Membership
- Pricing page
- Success page that calls POST /api/membership/activate

B) Events
- Category grid
- Category listing
- Event detail page (video, thumbnail, donation button)

C) Creator Tools
- Create Event (thumbnail + video upload)
- My Events
- Dashboard
- Donations list
- Revenue pool earnings

D) Contests
- Contest list
- My entries
- Entry button only for member_30 or creator_50

E) Donations
- Donation success page that records donation

F) Navigation
- Free: Home, Events, Categories, Contests, Membership
- Member: Home, Events, Categories, Contests, Dashboard
- Creator: Home, Events, Categories, Create Event, My Events, Dashboard, Contests

────────────────────────────────────────
3. BACKEND CLEANUP + IMPLEMENTATION
────────────────────────────────────────

DO NOT REMOVE:
- Stripe SDK
- Stripe portal session endpoint
- ffmpeg
- sharp

Remove:
- Old webhook logic
- Old subscription sync logic
- Old event request logic
- Old contest approval logic
- Any unused backend routes

Implement or fix backend routes:

POST /api/membership/activate
- Reads { tier }
- Updates profiles table
- Sets membership_active, membership_tier, membership_started_at

POST /api/events/create
- Validates creator_50
- Uploads thumbnail + video URLs
- Inserts into event_slots

GET /api/events/list
GET /api/events/:id

POST /api/donations/record
- Validate user
- Validate event_id
- Insert donation record
- Insert revenue_pool_entries (source: donation)

POST /api/contests/enter
- Validate membership tier
- Insert contest entry

────────────────────────────────────────
4. DATABASE SYNC (Supabase)
────────────────────────────────────────

Ensure tables exist:

profiles
event_slots
donations
revenue_pool
revenue_pool_entries
contest_entries

Ensure RLS policies:
- Public read for events
- Insert/update/delete only by creator for event_slots
- Contest entry only for member_30 or creator_50
- Donations insert allowed for authenticated users

────────────────────────────────────────
5. FINAL INTEGRATION + LAUNCH READINESS
────────────────────────────────────────

After rebuilding:

- Ensure all pages compile
- Ensure all routes work
- Ensure membership activation works
- Ensure event creation works
- Ensure event detail works
- Ensure category pages show events
- Ensure contests work
- Ensure donations work
- Ensure revenue pool updates
- Ensure navigation updates based on tier
- Ensure Stripe portal still works
- Ensure ffmpeg denoise still works
- Ensure no old logic remains
- Ensure the user’s recent manual changes are preserved and integrated

Deliver a fully functional, launch-ready Studio Flow 2.0.

Make all changes now.
