You are updating the Studio Flow platform to unify the Supabase schema, fix all Events and Contests functionality, restore admin visibility, remove dashboard errors, and wire the Donate button to the correct Stripe link.

Perform the following tasks:

────────────────────────────────────────
1. FRONTEND ADMIN VISIBILITY
────────────────────────────────────────

A. Ensure the frontend loads the user's role correctly:
- After supabase.auth.getUser(), fetch the profile row from the profiles table.
- Merge profile.role into the user object.
- Store user.role in global auth context.

B. Update all admin UI conditionals:
Replace any checks like:
  user?.isAdmin
with:
  user?.role === 'creator_admin'

C. Ensure the sidebar shows:
- Custom Events
- Contests
- Submissions
- Announcements
- Academy
- Subscription
ONLY when user.role === 'creator_admin'.

D. Ensure each admin page renders its content instead of showing empty screens.

────────────────────────────────────────
2. BACKEND ADMIN ROUTES
────────────────────────────────────────

A. Update all admin routes to check:
  if (user.role !== 'creator_admin') return 403

B. Ensure the following routes exist and return data:
- /api/admin/contests
- /api/admin/events
- /api/admin/announcements
- /api/admin/submissions
- /api/admin/categories
- /api/admin/academy
- /api/admin/subscription

C. Ensure these routes query the correct tables and return full data sets.

────────────────────────────────────────
3. EVENTS SYSTEM FIXES
────────────────────────────────────────

A. Replace all event_type references with event_mode.

B. Ensure filtering works:
  ?type=live → event_mode = 'live'
  ?type=recorded → event_mode = 'recorded'
  ?type=upcoming → event_date > now()
  ?type=ended → event_date < now()

C. Ensure event creation supports:
- stream_key
- stream_url
- video_url
- live_room_id

D. Ensure event_slots table uses event_mode.

────────────────────────────────────────
4. CONTESTS, ANNOUNCEMENTS, SUBMISSIONS, ACADEMY, SUBSCRIPTION
────────────────────────────────────────

A. Ensure each admin page:
- Fetches data from its admin route
- Renders lists of existing items
- Shows Create / Edit / Delete / Approve buttons
- Does not return empty screens unless the table is truly empty

B. Fix any missing components or broken imports.

────────────────────────────────────────
5. DONATE BUTTON — WIRE TO STRIPE
────────────────────────────────────────

Locate the Donate button on the homepage and replace its current href/onClick with:

<a
  href="https://buy.stripe.com/dRmaEZehf3JSfnm84Rb7y0p"
  target="_blank"
  rel="noopener noreferrer"
  className="donate-button"
>
  Donate
</a>

Ensure the button is visible on the homepage and styled consistently.

────────────────────────────────────────
6. CLEANUP
────────────────────────────────────────

- Remove all deprecated event_type code.
- Remove unused components.
- Remove old admin logic.
- Ensure no page returns empty arrays unless data is actually empty.
- Ensure all admin pages load real data.

────────────────────────────────────────
7. VALIDATION
────────────────────────────────────────

Confirm:
- Admin sidebar shows all admin pages.
- Each admin page loads real data.
- Events dashboard works.
- Live/Recorded workflow works.
- Donate button opens the Stripe link.
- No empty pages unless data is actually empty.

────────────────────────────────────────
8. COMMIT & PUSH
────────────────────────────────────────

Commit all changes and push to GitHub.
