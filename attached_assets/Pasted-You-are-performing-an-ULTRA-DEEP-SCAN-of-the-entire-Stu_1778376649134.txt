You are performing an ULTRA-DEEP SCAN of the entire Studio Flow frontend repository. 
Your job is to locate and remove ALL outdated, duplicated, or incorrect contest/event UI, 
ALL incorrect API paths, ALL unused sidebar/dashboard files, and ANY Netlify rewrite conflicts.

GOAL:
1. Remove ALL static contest items under “Subscription”.
2. Remove ALL old hard-coded Events from ANY dashboard or home component.
3. Remove ALL old hard-coded Contests from ANY dashboard or home component.
4. Remove ALL outdated contest/event components, routes, imports, and JSON files.
5. Remove ALL references to the nonexistent Supabase table “profiles”.
6. Fix ALL incorrect API paths (hardcoded `/api/...`).
7. Ensure ALL API calls use: import.meta.env.VITE_API_BASE_URL
8. Ensure contests appear ONLY at /contests (API-driven).
9. Ensure events appear ONLY at /events (API-driven).
10. Ensure the sidebar contains ONLY the correct items.
11. Ensure no duplicate sidebar/dashboard files exist.
12. Ensure no Netlify rewrite conflicts exist in the codebase.

RULES:
- Modify ONLY frontend files.
- Do NOT modify backend code.
- Do NOT modify Supabase.
- Do NOT commit any test files.
- All changes must be permanent and committed.

TASKS:

1. FULL REPO SEARCH (MANDATORY)
   Search the ENTIRE repository recursively for the following strings:
     “Funniest Baby Moments”
     “Worlds Funniest Video”
     “Creator Showcase Night”
     “Beat Battle Live”
     “Comedy Open Mic”
     “Art & Design Expo”
     “Producer Workshop”
     “Freestyle Friday”
     “profiles?select”
     “/rest/v1/profiles”
     “/api/”
     “Sidebar”
     “Dashboard”
     “Events”
     “Contests”
   For EACH file containing ANY of these:
     - Remove static contest/event blocks.
     - Remove unused components.
     - Remove unused imports.
     - Remove outdated routes.
     - Remove mock data.
     - Remove duplicate sidebar or dashboard definitions.

2. SIDEBAR DEEP CLEANUP
   - Search for ALL files named:
       Sidebar.jsx
       Sidebar.tsx
       sidebar.js
       sidebar.ts
       layout/Sidebar.jsx
       ui/Sidebar.jsx
   - Open each file.
   - DELETE all static contest entries under “Subscription”.
   - Ensure the ONLY contest link is:
       { name: "Contests", route: "/contests" }
   - Ensure the ONLY events link is:
       { name: "Events", route: "/events" }

3. DASHBOARD DEEP CLEANUP
   - Search for ALL files named:
       Dashboard.jsx
       Dashboard.tsx
       Home.jsx
       Home.tsx
       Hub.jsx
       Overview.jsx
       MainDashboard.jsx
   - DELETE ALL old event blocks.
   - DELETE ALL old contest blocks.
   - DELETE ALL static cards referencing old events or contests.
   - Dashboard should show ONLY:
       - Status widgets
       - Quick links
       - Optional summary counts

4. ROUTER DEEP CLEANUP
   - Search for ALL router files:
       App.jsx
       App.tsx
       routes/index.jsx
       routes/index.tsx
       router.jsx
       router.tsx
   - Remove ANY route referencing old contest/event components.
   - Ensure ONLY these routes exist:
       /contests
       /contests/:contestId
       /events
       /events/:eventId

5. API PATH DEEP CLEANUP
   - Search for ALL occurrences of:
       "/api/"
       "axios.get('/api"
       "fetch('/api"
   - Replace ALL of them with:
       `${import.meta.env.VITE_API_BASE_URL}/api/...`
   - Remove ANY fallback logic that defaults to `/api`.

6. NETLIFY REWRITE CHECK
   - Search the repo for:
       netlify.toml
       _redirects
   - If ANY redirect rewrites `/api/*` to frontend HTML or Netlify functions:
       DELETE the redirect.

7. COMPONENT CLEANUP
   - Delete ANY unused components found in step 1.
   - Delete ANY static JSON files containing old contest/event data.
   - Delete ANY mock data files for contests/events.

8. VERIFY
   - Run the frontend locally.
   - Confirm:
       - Sidebar has NO contests under Subscription.
       - Dashboard shows NO events and NO contests.
       - /contests shows ONLY API-driven contests.
       - /events shows ONLY API-driven events.
       - No duplicates anywhere.
       - No 404s from nonexistent Supabase tables.
       - No JSON parse errors.
       - All API calls hit the correct backend URL.

9. Commit all cleaned-up frontend files.
