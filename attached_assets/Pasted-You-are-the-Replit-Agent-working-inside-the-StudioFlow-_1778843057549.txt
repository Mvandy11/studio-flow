You are the Replit Agent working inside the StudioFlow codebase. Your task is to fix all Netlify serverless functions so that authentication, session validation, and API routes stop crashing. StudioFlow login is currently stuck in a loading state because Netlify functions are failing with a module import error.

────────────────────────────────────────
0. ROOT ISSUE
────────────────────────────────────────
Netlify cannot run native modules like fluent-ffmpeg. The file:

netlify/functions/api.js

contains an import such as:

import ffmpeg from 'fluent-ffmpeg';

This causes Netlify to crash every function at runtime, which breaks:
- Supabase Auth session handshake
- Admin login
- Customer login
- Profile loading
- Membership loading
- All API routes

Your job is to remove or safely lazy-load this import.

────────────────────────────────────────
1. REMOVE OR LAZY-LOAD FFMPEG
────────────────────────────────────────
Open netlify/functions/api.js and:

- Remove any top-level import of fluent-ffmpeg
- Remove any top-level require('fluent-ffmpeg')
- If ffmpeg is needed later, wrap it in:

const ffmpeg = await import('fluent-ffmpeg');

But only if the code path is actually used. Otherwise remove it entirely.

Ensure the file no longer references ffmpeg at module load time.

────────────────────────────────────────
2. VERIFY ALL NETLIFY FUNCTIONS BUILD
────────────────────────────────────────
Check the following directories:

- netlify/functions/
- netlify/functions/api.js
- netlify/functions/*.js

Ensure:
- No native modules are imported
- No missing dependencies
- No invalid ESM/CommonJS mixing
- No references to modules not in package.json

Fix any issues.

────────────────────────────────────────
3. VERIFY SUPABASE CLIENT USAGE
────────────────────────────────────────
Ensure serverless functions use:

import { createClient } from '@supabase/supabase-js';

with:

const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);

Confirm:
- Keys are loaded from Netlify env vars
- No undefined env vars
- No missing keys

────────────────────────────────────────
4. CLEAN UP DEAD CODE
────────────────────────────────────────
Remove any unused imports or unreachable code that may cause bundling errors.

────────────────────────────────────────
5. OUTPUT SUMMARY
────────────────────────────────────────
At the end, output:

- Which files were modified
- Whether ffmpeg imports were removed
- Whether Netlify functions now build cleanly
- Whether any additional fixes were applied
- Any TODOs remaining
