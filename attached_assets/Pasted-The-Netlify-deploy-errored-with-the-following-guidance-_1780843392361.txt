The Netlify deploy errored, with the following guidance provided:

Diagnosis
- I can’t isolate the failing error because no build log lines were included. Without the specific error lines I cannot identify the exact error type or root cause.

Solution (what to do next and immediate troubleshooting steps)
1. Get the exact error lines
   - Open the Netlify deploy log and copy the failing lines (the portion around the first error and the stack trace). Paste those lines here. If you prefer to reproduce locally, run:
```bash
# from repo root
npm install
npm run build
# or the exact build command you configured on Netlify
```
   - If you use the Netlify CLI you can also run:
```bash
npx netlify-cli build
```
   - When you paste the log, include ~10–20 lines before and after the first error so I can isolate the cause.

2. Common quick checks you can do immediately
   - Verify package.json and committed files:
     - Confirm the package named in the error (if it's a missing package) appears in package.json dependencies or devDependencies and that package-lock.json / yarn.lock is up-to-date.
     - Confirm any file referenced in the error actually exists in the repository and was committed.
   - If the error mentions Node / Node.js or incompatible syntax, check Netlify’s Node version:
     - Ensure the Node version in your repo matches Netlify’s build runtime. You can pin Node by adding an .nvmrc or adding "engines" in package.json. See Netlify docs: https://docs.netlify.com/configure-builds/manage-dependencies/#node-js-and-javascript

3. If the error is an unresolved dependency when bundling (Rollup/Vite) — how I will advise when you confirm
   - First verify the package is present in package.json or the referenced file is committed to the repo.
   - If verified and you still get an error like “Could not resolve 'some-package'” during bundling and you need to mark it external, edit your Vite/Rollup config to externalize it. Example Vite config snippet:
```js
// vite.config.js
import { defineConfig } from 'vite'

export default defineConfig({
  build: {
    rollupOptions: {
      external: ['some-package'] // only after you confirmed it's in package.json or file is committed
    }
  }
})
```
   - Note: Do the verification step above before adding to external; externalizing a package removes it from the bundle and requires it to be available at runtime.

4. If you see an error about native Node modules (fs/path) in client build
   - That usually means server-side code is being bundled for the browser. Identify where Node-only modules are imported and move that code to server-only contexts or use dynamic conditional imports so the client bundle does not include them.

5. When you paste the failing log lines
   - I will:
     1. isolate the exact error lines from your log,
     2. identify the error type and cause,
     3. provide a precise code/config change with file path and exact lines to edit.

If you paste the relevant log lines now I’ll diagnose the exact failure and give the specific fix.

The relevant error logs are:
