You are updating the Studio Flow project. Implement the “Potential Earnings Calculator” feature that appears on the Event Creation page and shows creators how much they can earn from ticket sales.

GOAL:
Add a calculator UI that dynamically shows:
1. Break-even point (how many tickets cover the membership cost).
2. Potential profit at different ticket quantities.
3. A visual earnings bar or chart.
4. A clear message: “Most creators earn back their membership with just X tickets.”

FILES TO CREATE OR MODIFY:
1. src/pages/events/CreateEventPage.jsx
2. src/components/events/EarningsCalculator.jsx (new)
3. src/lib/calcEarnings.js (new helper)

REQUIREMENTS:

A) Create src/lib/calcEarnings.js:
- export function calculateEarnings({ ticketPrice, membershipCost, quantity })
- return:
  {
    revenue: ticketPrice * quantity,
    breakEvenTickets: Math.ceil(membershipCost / ticketPrice),
    profit: (ticketPrice * quantity) - membershipCost
  }

B) Create src/components/events/EarningsCalculator.jsx:
- Props: ticketPrice, membershipCost (default 15), onChangeQuantity
- UI elements:
  - Input slider or number input for “Expected Ticket Sales”
  - Display:
      - “Break-even: X tickets”
      - “Revenue: $Y”
      - “Profit: $Z”
  - A small bar chart or progress bar showing revenue vs membership cost
  - A motivational line:
      “You profit after X tickets.”
- Update dynamically as the user changes ticket quantity.

C) Modify CreateEventPage.jsx:
- Import EarningsCalculator
- When user sets ticket price, show the calculator below the price input
- Pass ticketPrice and membershipCost (use 10 or 15 depending on tier)
- Default expected quantity = 10
- Add a section header: “Potential Earnings”

D) UI Style:
- Match Studio Flow’s cinematic UI
- Use soft gradients, smooth transitions, and bold numbers
- Keep the calculator compact and visually motivating

E) Logic:
- If ticketPrice is empty or zero, hide the calculator
- If event is free, hide the calculator
- If membership tier is unknown, default membershipCost = 15

F) DO NOT modify database schema or RLS. Only implement client-side UI and helper logic.

After implementing, show me the diff of all changed files.
