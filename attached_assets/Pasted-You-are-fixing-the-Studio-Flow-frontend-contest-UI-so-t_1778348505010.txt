You are fixing the Studio Flow frontend contest UI so that all contests come from a single source of truth and appear in only one location.

GOAL:
Unify the contest UI by removing old static contest lists, removing duplicates under Subscription, removing outdated dashboard contest blocks, and ensuring the only contest list comes from GET /api/contests at the /contests route.

RULES:
- Do NOT modify backend code.
- Do NOT modify API routes.
- Do NOT commit any test files.
- Only modify frontend files needed to clean up the UI.

TASKS:

1. CLEAN UP SIDEBAR
   - Open the sidebar configuration file (likely src/components/Sidebar.jsx or src/config/sidebar.js).
   - Remove ALL static contest entries under the "Subscription" section, such as:
       "Funniest Baby Moments"
       "Worlds Funniest Video"
   - Ensure the only contest-related sidebar item is:
       { name: "Contests", route: "/contests" }
   - Do NOT fetch contests in the sidebar.

2. CLEAN UP DASHBOARD
   - Open the Dashboard component (likely src/pages/Dashboard.jsx).
   - Remove the entire old "Monthly Contests" or "Featured Contests" block.
   - Remove any hard-coded contest cards or lists.
   - Replace with a simple status section:
       - "Active Contests: X" (link to /contests)
       - "Submissions Pending: Y" (optional)
       - "Unread Notifications: Z" (optional)
   - Do NOT fetch contests directly in the dashboard.

3. FIX ROUTING
   - Open your router file (likely src/App.jsx or src/routes/index.jsx).
   - Ensure ONLY these contest routes exist:
       <Route path="/contests" element={<ContestsPage />} />
       <Route path="/contests/:contestId" element={<ContestDetailsPage />} />
   - Remove any old or duplicate routes such as:
       /contest
       /contest-list
       /creative
       /subscription/contest/*
       /events/contest/*
   - Ensure no other components fetch contests.

4. FIX CONTESTS PAGE
   - Open src/pages/ContestsPage.jsx.
   - Ensure contests are fetched ONLY from:
       GET `${import.meta.env.VITE_API_BASE_URL}/api/contests`
   - Ensure categories, filters, and status chips use the data returned from the API.
   - Ensure no fallback static contest list exists.

5. FIX CONTEST DETAILS PAGE
   - Open src/pages/ContestDetailsPage.jsx.
   - Ensure contest details load from:
       GET `${import.meta.env.VITE_API_BASE_URL}/api/contests/${contestId}`
   - Ensure entries load from:
       GET `${import.meta.env.VITE_API_BASE_URL}/api/contests/${contestId}/entries`
   - Remove any static placeholder content.

6. CLEAN UP OLD FILES
   - Search the codebase for any file containing "Funniest Baby Moments" or "Worlds Funniest Video".
   - Remove or update any leftover static contest references.
   - Remove any unused components like OldContestList.jsx or ContestCardStatic.jsx.

7. VERIFY
   - Run the frontend locally.
   - Confirm:
       - Sidebar shows only "Contests"
       - Dashboard shows no contest list
       - /contests shows all contests from Supabase
       - /contests/:id shows correct details
       - No duplicates anywhere

8. Commit only the cleaned-up frontend files.
