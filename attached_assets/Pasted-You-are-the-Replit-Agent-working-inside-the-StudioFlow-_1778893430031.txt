You are the Replit Agent working inside the StudioFlow codebase. Your task is to validate and repair the frontend admin gating logic so that the admin UI only appears for users whose profile.role === 'admin', and always appears for valid admins.

Perform the following:

────────────────────────────────────────
1. LOCATE ADMIN GATING LOGIC
────────────────────────────────────────
Search for:
- supabase.auth.getUser()
- supabase.from("profiles").select(...)
- role checks such as: profile.role !== 'admin'
- redirect('/') or navigate('/')

Identify all components/pages that gate admin access, including:
- AdminSidebar
- AdminDashboard
- AdminContestPage
- AdminSettings
- Any route-level guards

────────────────────────────────────────
2. VALIDATE PROFILE FETCH LOGIC
────────────────────────────────────────
Ensure the frontend fetches the profile using:
const { data: { user } } = await supabase.auth.getUser();
const { data: profile } = await supabase.from("profiles").select("*").eq("id", user.id).single();

Check for:
- Missing .single()
- Missing await
- Incorrect column names
- Incorrect table names
- Incorrect id comparisons
- Silent failures (profile === null)

Fix any issues.

────────────────────────────────────────
3. VALIDATE ADMIN CHECK
────────────────────────────────────────
Ensure the admin check is EXACTLY:

if (profile?.role !== 'admin') {
  redirect('/');
}

Confirm:
- No typos
- No mismatched casing
- No undefined profile access
- No fallback that hides admin UI

────────────────────────────────────────
4. VALIDATE LOADING STATES
────────────────────────────────────────
Ensure the UI does NOT hide admin components while:
- user is loading
- profile is loading
- supabase is initializing

Fix any logic like:
if (!profile) return null;

Replace with:
if (loading) return <LoadingSpinner />;

────────────────────────────────────────
5. VALIDATE ADMIN SIDEBAR VISIBILITY
────────────────────────────────────────
Ensure the Admin tab appears ONLY when:
profile.role === 'admin'

Check:
- Sidebar component
- Mobile menu
- Any conditional rendering

────────────────────────────────────────
6. OUTPUT SUMMARY
────────────────────────────────────────
At the end, output:
- Files inspected
- Files modified
- Exact admin gating logic used
- Any bugs found and fixed
- Confirmation that admin UI now appears for valid admins
