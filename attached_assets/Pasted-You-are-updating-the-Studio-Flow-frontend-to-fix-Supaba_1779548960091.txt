You are updating the Studio Flow frontend to fix Supabase authentication,
session persistence, and profile loading. The current issue is that the
frontend is making Supabase requests without a valid session token, causing
401 and 404 errors, and preventing subscription status, profiles, and events
from loading.

Make the following changes:

────────────────────────────────────────
1. FIX SUPABASE CLIENT INITIALIZATION
────────────────────────────────────────

Locate the Supabase client file:
- src/lib/supabase.js
- or src/utils/supabaseClient.js
- or src/supabaseClient.js

Replace the existing createClient call with:

import { createClient } from '@supabase/supabase-js'

export const supabase = createClient(
  import.meta.env.VITE_SUPABASE_URL,
  import.meta.env.VITE_SUPABASE_ANON_KEY,
  {
    auth: {
      persistSession: true,
      autoRefreshToken: true,
      detectSessionInUrl: true
    }
  }
)

This ensures:
- JWT is attached to every request
- Session persists across reloads
- Session auto-refreshes
- OAuth redirects work

────────────────────────────────────────
2. FIX SESSION LOADING BEFORE PROFILE FETCH
────────────────────────────────────────

Find any code that loads the profile, such as:

const { data: profile } = await supabase
  .from("profiles")
  .select("*")
  .eq("id", user.id)
  .single()

Before this call, insert:

const { data: { session } } = await supabase.auth.getSession()
if (!session) return null

This prevents 401 errors when the session isn't ready yet.

────────────────────────────────────────
3. FIX AUTH STATE LISTENER
────────────────────────────────────────

Ensure the app listens for auth changes:

supabase.auth.onAuthStateChange((event, session) => {
  if (session) {
    localStorage.setItem("sb-session", JSON.stringify(session))
  } else {
    localStorage.removeItem("sb-session")
  }
})

This ensures the UI updates when the user logs in/out.

────────────────────────────────────────
4. FIX PROFILE FETCHING HOOK / CONTEXT
────────────────────────────────────────

If you have a user context or profile loader, update it to:

async function loadProfile() {
  const { data: { session } } = await supabase.auth.getSession()
  if (!session) return null

  const { data, error } = await supabase
    .from("profiles")
    .select("*")
    .eq("id", session.user.id)
    .single()

  return data
}

This ensures the correct user ID is used.

────────────────────────────────────────
5. VERIFY ENVIRONMENT VARIABLES
────────────────────────────────────────

Ensure the frontend uses:

VITE_SUPABASE_URL=https://uhajakhxmobhpesqdzth.supabase.co
VITE_SUPABASE_ANON_KEY=<your real anon key>

Do NOT modify the key — it is correct.

────────────────────────────────────────
6. TESTING CHECKLIST
────────────────────────────────────────

After applying the patch, verify:

- No more 401 errors in console
- No more 404 errors on profiles
- Subscription status loads correctly
- Profile loads correctly
- Events load correctly
- Creator tools appear when logged in
- Viewer tools appear when logged out
- Error Logs page no longer shows HTML parse errors

Make all changes now.
