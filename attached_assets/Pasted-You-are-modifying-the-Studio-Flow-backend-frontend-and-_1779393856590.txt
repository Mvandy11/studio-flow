You are modifying the Studio Flow backend, frontend, and Supabase schema. Fix the event approval workflow so that approving an event request correctly creates an event slot with a valid stream_key. The current error is:

"Could not find the 'stream_key' column of 'event_slots' in the schema cache."

This means the frontend and backend expect a stream_key column, but the Supabase table does not have it. Fix this end-to-end.

────────────────────────────
SUPABASE SCHEMA FIX
────────────────────────────

1. Add the missing column to event_slots:

   alter table event_slots
   add column stream_key text;

2. (Optional but recommended) Add a stream_url column:

   alter table event_slots
   add column stream_url text;

3. Ensure RLS allows service role inserts:
   No changes needed if supabaseAdmin is used.

────────────────────────────
BACKEND FIX (Node / Express)
────────────────────────────

4. Open routes/adminApproval.js (or the file handling event request approvals).

5. Update the slot creation logic so it includes stream_key:

   const streamKey = crypto.randomUUID();

   await supabaseAdmin.from("event_slots").insert({
     event_id: event.id,
     slot_title,
     upload_password,
     stream_key: streamKey,
     stream_url: null
   });

6. Ensure the backend does NOT attempt to write any fields that do not exist in the schema.

7. Ensure the backend returns the created slot and event to the frontend.

────────────────────────────
FRONTEND FIX (React)
────────────────────────────

8. Open the event approval modal component.

9. Ensure the frontend sends ONLY these fields to the backend:
   - slot_title
   - upload_password

   The backend must generate stream_key automatically.

10. Remove any frontend code that tries to generate or send stream_key.

11. Ensure the UI displays the stream key returned from the backend (if needed).

────────────────────────────
VERIFICATION
────────────────────────────

12. Approve an event request and confirm:
   - No schema cache errors
   - Slot is created successfully
   - Event is created successfully
   - stream_key exists in event_slots
   - Approval modal closes without errors

Make all changes now.
