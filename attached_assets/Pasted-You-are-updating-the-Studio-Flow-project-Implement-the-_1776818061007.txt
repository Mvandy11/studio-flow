You are updating the Studio Flow project. Implement the full Step E “Ticket Purchase UI + Payment Integration” flow.

GOAL:
Create a complete purchase experience for paid events. When a user tries to join a paid event without a ticket, they should be redirected to a purchase page where they can buy a ticket. After successful payment, insert a row into event_tickets and redirect the user back to the event.

FILES TO CREATE OR MODIFY:
1. src/pages/events/PurchasePage.jsx (new)
2. src/pages/events/EventPage.jsx (ensure redirect to purchase page already exists from Step D)
3. src/lib/createTicket.js (new helper)
4. src/components/payments/CheckoutButton.jsx (new)
5. Add route for /events/:eventId/purchase in router

REQUIREMENTS:

A) Create src/pages/events/PurchasePage.jsx:
- Fetch event by eventId
- Display event title, thumbnail, ticket price
- Show a “Buy Ticket” button using CheckoutButton component
- Show loading and error states
- After successful purchase, redirect to `/events/${eventId}` or directly to `/stage/${stageRoomId}`

B) Create src/components/payments/CheckoutButton.jsx:
- Accept props: eventId, price, user, onSuccess
- Simulate payment for now (no real Stripe integration yet)
- On click:
  - disable button
  - show “Processing…” state
  - call createTicket(eventId, user.id)
  - call onSuccess() after ticket is created

C) Create src/lib/createTicket.js:
- export async function createTicket(supabase, eventId, userId)
- Insert a row into event_tickets:
  {
    event_id: eventId,
    user_id: userId,
    payment_reference: "test-payment-" + Date.now()
  }
- Return the inserted ticket or throw an error

D) Update router:
- Add route: /events/:eventId/purchase → PurchasePage

E) UI Requirements:
- Keep the design cinematic and consistent with Studio Flow
- Use your existing layout components (PageContainer, Header, etc.)
- Include a back button to return to the event page
- Show price in a large, premium style
- Show a short description: “This ticket grants access to the Live Studio Stage.”

F) DO NOT modify RLS or database schema. Only implement the client-side purchase flow.

After implementing, show me the diff of all changed files.
