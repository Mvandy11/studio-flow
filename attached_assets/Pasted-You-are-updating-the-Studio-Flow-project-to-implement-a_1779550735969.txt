You are updating the Studio Flow project to implement a clean, reliable
membership system using Stripe Payment Links and Supabase profiles.

The goal: match each user to the correct membership tier (free, $30, $50)
based on the Payment Link they used, without relying on Stripe webhooks.

Make the following changes:

────────────────────────────────────────
1. REMOVE ALL STRIPE SUBSCRIPTION WEBHOOK LOGIC
────────────────────────────────────────

Delete or disable any code that handles:
- customer.subscription.created
- customer.subscription.updated
- invoice.paid
- invoice.payment_failed
- subscription metadata
- subscription status syncing

Studio Flow uses Stripe Payment Links, not Subscription objects.

────────────────────────────────────────
2. CREATE A NEW API ROUTE: /api/membership/activate
────────────────────────────────────────

Create a serverless function or backend route:

POST /api/membership/activate

It should:

- Read JSON body: { tier: "member_30" | "creator_50" }
- Read the authenticated Supabase user ID
- Update the user's profile in Supabase:

For member_30:
  membership_active = true
  membership_tier = "member_30"
  membership_started_at = now()

For creator_50:
  membership_active = true
  membership_tier = "creator_50"
  membership_started_at = now()

If no tier is provided:
  membership_active = false
  membership_tier = "free"

Return JSON: { success: true }

────────────────────────────────────────
3. UPDATE THE MEMBERSHIP SUCCESS PAGE
────────────────────────────────────────

Find the page at:
  /membership/success
(or create it if missing)

Add code to:

- Read the tier from the URL:
    const tier = new URLSearchParams(window.location.search).get("tier")

- Call the backend:
    await fetch("/api/membership/activate", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ tier })
    })

- After activation, redirect the user to their dashboard.

────────────────────────────────────────
4. UPDATE SUPABASE PROFILES TABLE
────────────────────────────────────────

Ensure the profiles table has:

membership_active (boolean, default false)
membership_tier (text, default 'free')
membership_started_at (timestamp, nullable)

If missing, create or alter these columns.

────────────────────────────────────────
5. UPDATE FRONTEND ACCESS LOGIC
────────────────────────────────────────

Wherever the app checks membership:

If membership_tier === "creator_50":
  Unlock creator dashboard, event creation, donations, uploads.

If membership_tier === "member_30":
  Unlock event viewing, chat, community.

If membership_tier === "free":
  Only allow free browsing and login.

────────────────────────────────────────
6. ADD PAYMENT LINK BUTTONS
────────────────────────────────────────

$30 Member Tier:
https://buy.stripe.com/7sYdRb2yx3JS1wwacZb7y0o
Success URL:
https://studioflow.tv/membership/success?tier=member_30

$50 Creator Tier:
https://buy.stripe.com/cNi6oJ1utcgo3EE4SFb7y0u
Success URL:
https://studioflow.tv/membership/success?tier=creator_50

Donation Link:
https://buy.stripe.com/28E14pgpncgofnmbh3b7y0t

Add these to the membership page and event pages.

────────────────────────────────────────
7. CLEAN UP OLD CODE
────────────────────────────────────────

Remove:
- Any references to subscription status
- Any Stripe subscription API calls
- Any webhook retry logic
- Any unused subscription tables

Studio Flow now uses Payment Links only.

────────────────────────────────────────
8. TESTING CHECKLIST
────────────────────────────────────────

After implementing:

- Free users show membership_tier = "free"
- $30 users show membership_tier = "member_30"
- $50 users show membership_tier = "creator_50"
- Creator dashboard unlocks correctly
- Event creation unlocks correctly
- Donations work
- No webhook errors
- No subscription errors
- No 401/404 membership fetch errors

Make all changes now.
