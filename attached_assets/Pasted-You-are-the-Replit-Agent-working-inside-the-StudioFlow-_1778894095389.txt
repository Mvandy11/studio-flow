You are the Replit Agent working inside the StudioFlow codebase. Your task is to build the full Winner Dashboard for admins. This dashboard must show:

- All winners for a contest
- All winners for an event
- Winner history (winner_history table)
- Place numbers
- Payout amounts
- User info (email or display name)
- Date won
- Buttons to pull new winners
- Buttons to refresh winner history

Perform the following tasks:

────────────────────────────────────────
1. CREATE /admin/winners PAGE
────────────────────────────────────────
Add a new admin page:
- /admin/winners
- /admin/contests/[id]/winners
- /admin/events/[id]/winners

Use a shared WinnerDashboard component.

────────────────────────────────────────
2. FETCH WINNER HISTORY
────────────────────────────────────────
Query:

supabase
  .from("winner_history")
  .select(`
    id,
    user_id,
    event_id,
    contest_id,
    place_number,
    payout_amount,
    created_at,
    profiles ( email, display_name )
  `)
  .order("place_number", { ascending: true });

Ensure RLS allows admins to read all rows.

────────────────────────────────────────
3. DISPLAY WINNER TABLE
────────────────────────────────────────
Render a table with:
- Place #
- Winner name or email
- Payout amount
- Date won
- Event ID
- Contest ID

Add sorting by:
- place_number
- created_at

────────────────────────────────────────
4. ADD “PULL WINNERS” BUTTON
────────────────────────────────────────
Add a button that calls:

POST /api/contests/:contestId/pull-winners

Body:
{
  eventId,
  numberOfWinners,
  payouts: [...]
}

Show:
- Loading state
- Success state
- Error state

After success:
- Refresh winner history

────────────────────────────────────────
5. ADD WINNER SUMMARY CARDS
────────────────────────────────────────
At the top of the dashboard, show:
- Total winners
- Total payout amount
- Last winner pulled
- Number of unique users

────────────────────────────────────────
6. ADD FILTERS
────────────────────────────────────────
Filters:
- By event
- By contest
- By date
- By place number

────────────────────────────────────────
7. ADD EMPTY STATES
────────────────────────────────────────
If no winners exist:
Show “No winners have been selected yet.”

────────────────────────────────────────
8. ADD ERROR STATES
────────────────────────────────────────
If Supabase returns an error:
Show a clear error message.

────────────────────────────────────────
9. OUTPUT SUMMARY
────────────────────────────────────────
At the end, output:
- Files created
- Files modified
- Components added
- API calls validated
- Confirmation that the Winner Dashboard is fully functional
