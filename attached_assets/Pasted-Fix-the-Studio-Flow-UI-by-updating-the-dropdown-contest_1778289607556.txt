Fix the Studio Flow UI by updating the dropdown contests, repairing the login button, and removing old contest names under Subscription. Follow these steps exactly and only modify the necessary files.

1. FIX DROPDOWN CONTESTS (SHOW NOTHING / BROKEN LINKS)
A. Remove all hard‑coded contest entries
In the dropdown menu component (Navbar, Sidebar, or Header), delete any static contest items such as:

jsx
<DropdownItem to="/contests/123">...</DropdownItem>
B. Fetch contests dynamically from Supabase
Add:

js
const { data: contests } = await supabase
  .from("contests")
  .select("id, title")
  .eq("status", "active");
C. Render contests dynamically
Replace the old contest list with:

jsx
{contests?.map(contest => (
  <DropdownItem key={contest.id} to={`/contests/${contest.id}`}>
    {contest.title}
  </DropdownItem>
))}
D. Ensure correct routing
Dropdown must use:

Code
/contests
/contests/:contestId
Remove any old contest routes.

2. FIX LOGIN BUTTON (NOT DOING ANYTHING)
Locate the login button in the Navbar/Header.

A. If it uses a <Link> with no route, fix it:
Replace:

jsx
<Link>Log In</Link>
with:

jsx
<Link to="/login">Log In</Link>
B. If login uses a modal, reconnect the handler:
jsx
onClick={() => setLoginOpen(true)}
C. Ensure the router includes:
jsx
<Route path="/login" element={<LoginPage />} />
Do not modify authentication logic — only reconnect the UI.

3. REMOVE EXTRA CONTEST NAMES UNDER SUBSCRIPTION
In the sidebar or subscription menu component:

A. Remove any static contest names such as:
“Funniest Baby Moments”

“World’s Funniest Video”

Duplicate entries

Delete any hard‑coded list like:

jsx
<li>Funniest Baby Moments</li>
<li>World’s Funniest Video</li>
B. Replace with nothing OR a clean placeholder:
jsx
{/* No contest categories under Subscription */}
Do NOT fetch contests here unless explicitly needed.

4. CLEAN UP OLD CATEGORY LOGIC
Ensure the sidebar and dropdown only use:

js
export const categories = [
  {
    id: "custom-events",
    title: "Custom Events",
    route: "/custom-event-request"
  },
  {
    id: "contests",
    title: "Contests",
    route: "/contests"
  },
  {
    id: "submissions",
    title: "Submissions",
    route: "/submissions"
  }
];
Remove:

Events

Education

DIY

Old contest categories

Old subscription contest categories

5. DO NOT MODIFY ANY OTHER SYSTEMS
Do NOT change:

Contest backend

Contest submissions

Custom Events

Submissions inbox

Live engine

Stripe logic

Only fix the UI issues listed above.

END PROMPT