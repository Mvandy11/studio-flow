You are updating the Studio Flow project. Apply the following patch to clean up the Profile page and replace the placeholder Shop section with real Studio Flow navigation links.

────────────────────────────────────────
1. REMOVE THE SHOP SECTION COMPLETELY
────────────────────────────────────────
Search the codebase for any of the following components or blocks:

- <ShopSection />
- <Products />
- <Shop />
- "Preset Pack"
- "Template Kit"
- "Sample Pack"
- Any JSX block containing a "Shop" header or Buy buttons

Delete the entire Shop section, including:
- The header
- The product cards
- The Buy buttons
- Any wrapper divs or components

Ensure no leftover imports remain.

────────────────────────────────────────
2. ADD REAL STUDIO FLOW LINKS TO THE PROFILE PAGE
────────────────────────────────────────
In the Profile page component (usually Profile.jsx, Profile.tsx, or pages/profile/index.jsx):

Below the profile header (avatar, username, stats, Edit Profile button), insert a new section:

A. Create a new <div> or <section> called "profile-links" or similar.

B. Add the following links as styled buttons or cards:

Creator Tools:
- My Submissions → /submissions
- My Contest Entries → /contests/my-entries
- My Events → /events/my-events
- My Tickets → /tickets
- My Earnings → /earnings

Platform Navigation:
- Contests → /contests
- Custom Events → /events
- Academy → /academy
- Announcements → /announcements

Account:
- Edit Profile → /profile/edit
- Log Out → existing logout handler

C. Use existing button or card styles from the project for consistency.

────────────────────────────────────────
3. ADD ADMIN-ONLY LINKS (CONDITIONAL)
────────────────────────────────────────
If user.role === 'creator_admin', add an Admin section:

Admin:
- Admin Dashboard → /admin
- Event Requests → /admin/event-requests
- Winner Approval → /admin/winner-approval
- Payouts → /admin/payouts

Wrap this block in a conditional:

{user?.role === 'creator_admin' && (
   ...admin links...
)}

────────────────────────────────────────
4. CLEAN UP UNUSED IMPORTS & COMPONENTS
────────────────────────────────────────
After removing the Shop section:
- Remove unused imports from the Profile component.
- Remove any leftover Shop-related components if they are no longer used anywhere.
- Ensure no console errors appear due to missing components.

────────────────────────────────────────
5. RETURN SUMMARY
────────────────────────────────────────
Return a summary of:
- All removed Shop components
- All added Studio Flow links
- Confirmation that admin links are conditional
- Confirmation that the Profile page is now clean and on-brand
