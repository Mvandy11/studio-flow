You are performing a full Supabase schema cleanup for the Studio Flow platform. 
Your goal is to identify and safely remove duplicate, unused, or legacy tables 
that may conflict with the backend API routes.

DO NOT delete any table unless it is confirmed to be unused.

PRIMARY TABLES THAT MUST REMAIN:
  - events
  - custom_event_requests
  - event_slots
  - submissions
  - videos
  - users (auth schema)
  - profiles (if used by auth)
  - contests
  - contest_entries
  - contest_votes

TASKS:

1. SCAN THE ENTIRE DATABASE
List ALL tables in the public schema.
Identify tables with names similar to:
  - events
  - event_slots
  - custom_event_requests
  - submissions
  - videos
  - contests
  - contest_entries
  - contest_votes

Flag any duplicates such as:
  - events_old
  - events_copy
  - events_backup
  - event_slots_old
  - event_slots_copy
  - submissions_old
  - videos_old
  - contests_old
  - *_backup
  - *_copy
  - *_test
  - *_temp

2. FOR EACH DUPLICATE TABLE:
  - Open the table definition.
  - Compare its columns to the real production table.
  - Check if ANY backend route references it.
  - Check if ANY RLS policies reference it.
  - Check if ANY foreign keys reference it.

If the table is NOT referenced anywhere:
  - Mark it as SAFE TO DELETE.

3. DELETE ONLY SAFE TABLES
For each table marked safe:
  - Drop the table using:
        drop table if exists <table_name> cascade;

4. CLEAN UP RLS POLICIES
Search for RLS policies referencing deleted tables.
Remove any orphaned policies.

5. CLEAN UP FOREIGN KEYS
Search for foreign keys referencing deleted tables.
Remove any orphaned constraints.

6. CLEAN UP FUNCTIONS & TRIGGERS
Search for:
  - functions
  - triggers
  - cron jobs
  - RPC endpoints
that reference deleted tables.
Remove or update them.

7. VERIFY FINAL SCHEMA
Ensure the ONLY event-related tables remaining are:
  - events
  - custom_event_requests
  - event_slots

Ensure the ONLY contest-related tables remaining are:
  - contests
  - contest_entries
  - contest_votes

Ensure the ONLY submission-related tables remaining are:
  - submissions
  - videos

8. EXPORT CLEAN SCHEMA
Generate a final schema dump for verification.

Commit all changes.
