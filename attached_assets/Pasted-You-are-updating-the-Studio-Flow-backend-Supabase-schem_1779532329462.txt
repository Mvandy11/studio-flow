You are updating the Studio Flow backend + Supabase schema to ensure the event_slots table matches what the backend expects. Fix ALL schema mismatches and ensure the backend no longer throws errors like:

"Could not find the 'hls_url' column of 'event_slots' in the schema cache"

────────────────────────────
1. UPDATE SUPABASE SCHEMA
────────────────────────────

Open a new Supabase SQL migration and ensure the following columns exist in event_slots:

Required columns:

- id uuid primary key default gen_random_uuid()
- event_id uuid references events(id)
- slot_title text
- upload_password text
- stream_key text
- stream_url text
- hls_url text
- status text default 'pending'
- created_at timestamptz default now()

Run the following SQL, which safely adds missing columns:

------------------------------------------------------------
alter table event_slots
add column if not exists stream_key text;

alter table event_slots
add column if not exists stream_url text;

alter table event_slots
add column if not exists hls_url text;

alter table event_slots
add column if not exists status text default 'pending';

alter table event_slots
add column if not exists created_at timestamptz default now();
------------------------------------------------------------

This SQL is idempotent and safe to run multiple times.

────────────────────────────
2. VERIFY BACKEND EXPECTATIONS
────────────────────────────

Open routes/adminApproval.js (or the file that creates event slots).

Ensure the slot insert includes:

{
  event_id,
  slot_title,
  upload_password,
  stream_key,
  stream_url,
  hls_url,
  status: "pending"
}

If any fields are missing, add them.

────────────────────────────
3. ADD LOGGING FOR DEBUGGING
────────────────────────────

Before inserting the slot, add:

console.log("Creating event slot with:", {
  event_id,
  slot_title,
  upload_password,
  stream_key,
  stream_url,
  hls_url
});

After insert:

console.log("Event slot created:", insertResult);

────────────────────────────
4. FIX 401 / 500 ERRORS
────────────────────────────

Ensure the admin approval route uses supabaseAdmin:

import supabaseAdmin from "../supabase/supabaseAdmin.js";

All inserts and updates MUST use supabaseAdmin, not the anon client.

────────────────────────────
5. FINAL VERIFICATION
────────────────────────────

After applying this patch:

- The hls_url error disappears
- Event approval works
- Slot creation works
- Stream key + URLs generate correctly
- No more 401 or 500 errors
- The admin modal closes normally

Make all changes now.
