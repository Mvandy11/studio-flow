[build]
  command   = "pnpm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "22"

# ── SPA catch-all (must be last) ───────────────────────────────
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
