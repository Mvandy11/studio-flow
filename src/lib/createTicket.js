/**
 * createTicket — stub kept for backwards compatibility.
 * The ticket system has been removed. This file is intentionally minimal.
 */
export async function createTicket(_supabase, _eventId, _userId) {
  return { ticket: null, freeTicket: null };
}
